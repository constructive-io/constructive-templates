/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCapKeys } from '../query-keys';
import { appLimitCapMutationKeys } from '../mutation-keys';
import type { AppLimitCapSelect, AppLimitCapWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLimitCapSelect, AppLimitCapWithRelations } from '../../orm/input-types';
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLimitCapMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLimitCapMutation<S extends AppLimitCapSelect>(
  params: {
    selection: {
      fields: S & AppLimitCapSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLimitCap: {
          appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLimitCap: {
      appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLimitCapMutation(
  params: {
    selection: SelectionConfig<AppLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitCapMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLimitCap.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLimitCapKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
