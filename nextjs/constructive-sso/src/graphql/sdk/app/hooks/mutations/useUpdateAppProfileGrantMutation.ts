/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileGrantKeys } from '../query-keys';
import { appProfileGrantMutationKeys } from '../mutation-keys';
import type {
  AppProfileGrantSelect,
  AppProfileGrantWithRelations,
  AppProfileGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfileGrantSelect,
  AppProfileGrantWithRelations,
  AppProfileGrantPatch,
} from '../../orm/input-types';
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppProfileGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appProfileGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppProfileGrantMutation<S extends AppProfileGrantSelect>(
  params: {
    selection: {
      fields: S & AppProfileGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppProfileGrant: {
          appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appProfileGrantPatch: AppProfileGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppProfileGrant: {
      appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appProfileGrantPatch: AppProfileGrantPatch;
  }
>;
export function useUpdateAppProfileGrantMutation(
  params: {
    selection: SelectionConfig<AppProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appProfileGrantPatch: AppProfileGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppProfileGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfileGrantMutationKeys.all,
    mutationFn: ({
      id,
      appProfileGrantPatch,
    }: {
      id: string;
      appProfileGrantPatch: AppProfileGrantPatch;
    }) =>
      getClient()
        .appProfileGrant.update({
          where: {
            id,
          },
          data: appProfileGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appProfileGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appProfileGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
