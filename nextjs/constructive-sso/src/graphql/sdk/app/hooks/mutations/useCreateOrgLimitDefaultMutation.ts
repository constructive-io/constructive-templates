/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitDefaultKeys } from '../query-keys';
import { orgLimitDefaultMutationKeys } from '../mutation-keys';
import type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  CreateOrgLimitDefaultInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  CreateOrgLimitDefaultInput,
} from '../../orm/input-types';
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgLimitDefaultMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgLimitDefaultMutation<S extends OrgLimitDefaultSelect>(
  params: {
    selection: {
      fields: S & OrgLimitDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgLimitDefault: {
          orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
        };
      },
      Error,
      CreateOrgLimitDefaultInput['orgLimitDefault']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgLimitDefault: {
      orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
    };
  },
  Error,
  CreateOrgLimitDefaultInput['orgLimitDefault']
>;
export function useCreateOrgLimitDefaultMutation(
  params: {
    selection: SelectionConfig<OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOrgLimitDefaultInput['orgLimitDefault']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitDefaultMutationKeys.create(),
    mutationFn: (data: CreateOrgLimitDefaultInput['orgLimitDefault']) =>
      getClient()
        .orgLimitDefault.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgLimitDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
