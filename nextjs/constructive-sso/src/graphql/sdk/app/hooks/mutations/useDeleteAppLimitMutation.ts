/**
 * Tracks per-actor usage counts against configurable maximum limits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitKeys } from '../query-keys';
import { appLimitMutationKeys } from '../mutation-keys';
import type { AppLimitSelect, AppLimitWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLimitSelect, AppLimitWithRelations } from '../../orm/input-types';
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLimitMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLimitMutation<S extends AppLimitSelect>(
  params: {
    selection: {
      fields: S & AppLimitSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLimit: {
          appLimit: InferSelectResult<AppLimitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLimit: {
      appLimit: InferSelectResult<AppLimitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLimitMutation(
  params: {
    selection: SelectionConfig<AppLimitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLimit.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLimitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
