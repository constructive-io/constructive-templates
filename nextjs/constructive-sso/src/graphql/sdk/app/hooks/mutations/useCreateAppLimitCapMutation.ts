/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCapKeys } from '../query-keys';
import { appLimitCapMutationKeys } from '../mutation-keys';
import type {
  AppLimitCapSelect,
  AppLimitCapWithRelations,
  CreateAppLimitCapInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLimitCapSelect,
  AppLimitCapWithRelations,
  CreateAppLimitCapInput,
} from '../../orm/input-types';
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppLimitCapMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppLimitCapMutation<S extends AppLimitCapSelect>(
  params: {
    selection: {
      fields: S & AppLimitCapSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppLimitCap: {
          appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
        };
      },
      Error,
      CreateAppLimitCapInput['appLimitCap']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppLimitCap: {
      appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
    };
  },
  Error,
  CreateAppLimitCapInput['appLimitCap']
>;
export function useCreateAppLimitCapMutation(
  params: {
    selection: SelectionConfig<AppLimitCapSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppLimitCapInput['appLimitCap']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppLimitCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitCapMutationKeys.create(),
    mutationFn: (data: CreateAppLimitCapInput['appLimitCap']) =>
      getClient()
        .appLimitCap.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appLimitCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
