/**
 * Template profiles that are automatically seeded into new entities when created
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileTemplateKeys } from '../query-keys';
import { orgProfileTemplateMutationKeys } from '../mutation-keys';
import type {
  OrgProfileTemplateSelect,
  OrgProfileTemplateWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileTemplateSelect,
  OrgProfileTemplateWithRelations,
} from '../../orm/input-types';
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgProfileTemplateMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgProfileTemplateMutation<S extends OrgProfileTemplateSelect>(
  params: {
    selection: {
      fields: S & OrgProfileTemplateSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileTemplateSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgProfileTemplate: {
          orgProfileTemplate: InferSelectResult<OrgProfileTemplateWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgProfileTemplate: {
      orgProfileTemplate: InferSelectResult<OrgProfileTemplateWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgProfileTemplateMutation(
  params: {
    selection: SelectionConfig<OrgProfileTemplateSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileTemplateSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileTemplateMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgProfileTemplate.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgProfileTemplateKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfileTemplateKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
