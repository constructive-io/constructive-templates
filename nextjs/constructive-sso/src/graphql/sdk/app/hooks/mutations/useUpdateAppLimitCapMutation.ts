/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCapKeys } from '../query-keys';
import { appLimitCapMutationKeys } from '../mutation-keys';
import type {
  AppLimitCapSelect,
  AppLimitCapWithRelations,
  AppLimitCapPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLimitCapSelect,
  AppLimitCapWithRelations,
  AppLimitCapPatch,
} from '../../orm/input-types';
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLimitCapMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appLimitCapPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppLimitCapMutation<S extends AppLimitCapSelect>(
  params: {
    selection: {
      fields: S & AppLimitCapSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppLimitCap: {
          appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appLimitCapPatch: AppLimitCapPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppLimitCap: {
      appLimitCap: InferSelectResult<AppLimitCapWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appLimitCapPatch: AppLimitCapPatch;
  }
>;
export function useUpdateAppLimitCapMutation(
  params: {
    selection: SelectionConfig<AppLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appLimitCapPatch: AppLimitCapPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitCapMutationKeys.all,
    mutationFn: ({ id, appLimitCapPatch }: { id: string; appLimitCapPatch: AppLimitCapPatch }) =>
      getClient()
        .appLimitCap.update({
          where: {
            id,
          },
          data: appLimitCapPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appLimitCapKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
