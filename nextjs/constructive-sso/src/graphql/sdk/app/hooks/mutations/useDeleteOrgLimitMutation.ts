/**
 * Tracks per-actor usage counts against configurable maximum limits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitKeys } from '../query-keys';
import { orgLimitMutationKeys } from '../mutation-keys';
import type { OrgLimitSelect, OrgLimitWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitSelect, OrgLimitWithRelations } from '../../orm/input-types';
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgLimitMutation<S extends OrgLimitSelect>(
  params: {
    selection: {
      fields: S & OrgLimitSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgLimit: {
          orgLimit: InferSelectResult<OrgLimitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgLimit: {
      orgLimit: InferSelectResult<OrgLimitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgLimitMutation(
  params: {
    selection: SelectionConfig<OrgLimitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgLimit.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgLimitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
