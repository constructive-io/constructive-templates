/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCapKeys } from '../query-keys';
import { orgLimitCapMutationKeys } from '../mutation-keys';
import type {
  OrgLimitCapSelect,
  OrgLimitCapWithRelations,
  CreateOrgLimitCapInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitCapSelect,
  OrgLimitCapWithRelations,
  CreateOrgLimitCapInput,
} from '../../orm/input-types';
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgLimitCapMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgLimitCapMutation<S extends OrgLimitCapSelect>(
  params: {
    selection: {
      fields: S & OrgLimitCapSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgLimitCap: {
          orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S>;
        };
      },
      Error,
      CreateOrgLimitCapInput['orgLimitCap']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgLimitCap: {
      orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S>;
    };
  },
  Error,
  CreateOrgLimitCapInput['orgLimitCap']
>;
export function useCreateOrgLimitCapMutation(
  params: {
    selection: SelectionConfig<OrgLimitCapSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgLimitCapInput['orgLimitCap']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgLimitCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitCapMutationKeys.create(),
    mutationFn: (data: CreateOrgLimitCapInput['orgLimitCap']) =>
      getClient()
        .orgLimitCap.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgLimitCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
