/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCreditKeys } from '../query-keys';
import { orgLimitCreditMutationKeys } from '../mutation-keys';
import type { OrgLimitCreditSelect, OrgLimitCreditWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitCreditSelect, OrgLimitCreditWithRelations } from '../../orm/input-types';
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitCreditMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgLimitCreditMutation<S extends OrgLimitCreditSelect>(
  params: {
    selection: {
      fields: S & OrgLimitCreditSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCreditSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgLimitCredit: {
          orgLimitCredit: InferSelectResult<OrgLimitCreditWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgLimitCredit: {
      orgLimitCredit: InferSelectResult<OrgLimitCreditWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgLimitCreditMutation(
  params: {
    selection: SelectionConfig<OrgLimitCreditSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitCreditSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitCreditMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgLimitCredit.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgLimitCreditKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitCreditKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
