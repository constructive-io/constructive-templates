/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appFileKeys } from '../query-keys';
import { appFileMutationKeys } from '../mutation-keys';
import type { AppFileSelect, AppFileWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppFileSelect, AppFileWithRelations } from '../../orm/input-types';
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppFileMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppFileMutation<S extends AppFileSelect>(
  params: {
    selection: {
      fields: S & AppFileSelect;
    } & HookStrictSelect<NoInfer<S>, AppFileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppFile: {
          appFile: InferSelectResult<AppFileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppFile: {
      appFile: InferSelectResult<AppFileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppFileMutation(
  params: {
    selection: SelectionConfig<AppFileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppFileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appFileMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appFile.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appFileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appFileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
