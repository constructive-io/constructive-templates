/**
 * User email addresses with verification and primary-email management
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailKeys } from '../query-keys';
import { emailMutationKeys } from '../mutation-keys';
import type { EmailSelect, EmailWithRelations, CreateEmailInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { EmailSelect, EmailWithRelations, CreateEmailInput } from '../../orm/input-types';
/**
 * User email addresses with verification and primary-email management
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateEmailMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateEmailMutation<S extends EmailSelect>(
  params: {
    selection: {
      fields: S & EmailSelect;
    } & HookStrictSelect<NoInfer<S>, EmailSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createEmail: {
          email: InferSelectResult<EmailWithRelations, S>;
        };
      },
      Error,
      CreateEmailInput['email']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createEmail: {
      email: InferSelectResult<EmailWithRelations, S>;
    };
  },
  Error,
  CreateEmailInput['email']
>;
export function useCreateEmailMutation(
  params: {
    selection: SelectionConfig<EmailSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateEmailInput['email']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<EmailSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailMutationKeys.create(),
    mutationFn: (data: CreateEmailInput['email']) =>
      getClient()
        .email.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: emailKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
