/**
 * Records of successfully claimed invitations, linking senders to receivers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgClaimedInviteKeys } from '../query-keys';
import { orgClaimedInviteMutationKeys } from '../mutation-keys';
import type {
  OrgClaimedInviteSelect,
  OrgClaimedInviteWithRelations,
  OrgClaimedInvitePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgClaimedInviteSelect,
  OrgClaimedInviteWithRelations,
  OrgClaimedInvitePatch,
} from '../../orm/input-types';
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgClaimedInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgClaimedInvitePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgClaimedInviteMutation<S extends OrgClaimedInviteSelect>(
  params: {
    selection: {
      fields: S & OrgClaimedInviteSelect;
    } & HookStrictSelect<NoInfer<S>, OrgClaimedInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgClaimedInvite: {
          orgClaimedInvite: InferSelectResult<OrgClaimedInviteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgClaimedInvitePatch: OrgClaimedInvitePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgClaimedInvite: {
      orgClaimedInvite: InferSelectResult<OrgClaimedInviteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgClaimedInvitePatch: OrgClaimedInvitePatch;
  }
>;
export function useUpdateOrgClaimedInviteMutation(
  params: {
    selection: SelectionConfig<OrgClaimedInviteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgClaimedInvitePatch: OrgClaimedInvitePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgClaimedInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgClaimedInviteMutationKeys.all,
    mutationFn: ({
      id,
      orgClaimedInvitePatch,
    }: {
      id: string;
      orgClaimedInvitePatch: OrgClaimedInvitePatch;
    }) =>
      getClient()
        .orgClaimedInvite.update({
          where: {
            id,
          },
          data: orgClaimedInvitePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgClaimedInviteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgClaimedInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
