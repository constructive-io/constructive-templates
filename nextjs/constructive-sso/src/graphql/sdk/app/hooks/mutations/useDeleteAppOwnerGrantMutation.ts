/**
 * Records of ownership transfers and grants between members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appOwnerGrantKeys } from '../query-keys';
import { appOwnerGrantMutationKeys } from '../mutation-keys';
import type { AppOwnerGrantSelect, AppOwnerGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppOwnerGrantSelect, AppOwnerGrantWithRelations } from '../../orm/input-types';
/**
 * Records of ownership transfers and grants between members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppOwnerGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppOwnerGrantMutation<S extends AppOwnerGrantSelect>(
  params: {
    selection: {
      fields: S & AppOwnerGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppOwnerGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppOwnerGrant: {
          appOwnerGrant: InferSelectResult<AppOwnerGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppOwnerGrant: {
      appOwnerGrant: InferSelectResult<AppOwnerGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppOwnerGrantMutation(
  params: {
    selection: SelectionConfig<AppOwnerGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppOwnerGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appOwnerGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appOwnerGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appOwnerGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appOwnerGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
