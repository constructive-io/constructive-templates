/**
 * Association table scoping principals to specific organizations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { principalEntityKeys } from '../query-keys';
import { principalEntityMutationKeys } from '../mutation-keys';
import type {
  PrincipalEntitySelect,
  PrincipalEntityWithRelations,
  CreatePrincipalEntityInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PrincipalEntitySelect,
  PrincipalEntityWithRelations,
  CreatePrincipalEntityInput,
} from '../../orm/input-types';
/**
 * Association table scoping principals to specific organizations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePrincipalEntityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePrincipalEntityMutation<S extends PrincipalEntitySelect>(
  params: {
    selection: {
      fields: S & PrincipalEntitySelect;
    } & HookStrictSelect<NoInfer<S>, PrincipalEntitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPrincipalEntity: {
          principalEntity: InferSelectResult<PrincipalEntityWithRelations, S>;
        };
      },
      Error,
      CreatePrincipalEntityInput['principalEntity']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPrincipalEntity: {
      principalEntity: InferSelectResult<PrincipalEntityWithRelations, S>;
    };
  },
  Error,
  CreatePrincipalEntityInput['principalEntity']
>;
export function useCreatePrincipalEntityMutation(
  params: {
    selection: SelectionConfig<PrincipalEntitySelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreatePrincipalEntityInput['principalEntity']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PrincipalEntitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: principalEntityMutationKeys.create(),
    mutationFn: (data: CreatePrincipalEntityInput['principalEntity']) =>
      getClient()
        .principalEntity.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: principalEntityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
