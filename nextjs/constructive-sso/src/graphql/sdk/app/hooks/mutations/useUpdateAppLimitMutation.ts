/**
 * Tracks per-actor usage counts against configurable maximum limits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitKeys } from '../query-keys';
import { appLimitMutationKeys } from '../mutation-keys';
import type { AppLimitSelect, AppLimitWithRelations, AppLimitPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLimitSelect, AppLimitWithRelations, AppLimitPatch } from '../../orm/input-types';
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLimitMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appLimitPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppLimitMutation<S extends AppLimitSelect>(
  params: {
    selection: {
      fields: S & AppLimitSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppLimit: {
          appLimit: InferSelectResult<AppLimitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appLimitPatch: AppLimitPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppLimit: {
      appLimit: InferSelectResult<AppLimitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appLimitPatch: AppLimitPatch;
  }
>;
export function useUpdateAppLimitMutation(
  params: {
    selection: SelectionConfig<AppLimitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appLimitPatch: AppLimitPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitMutationKeys.all,
    mutationFn: ({ id, appLimitPatch }: { id: string; appLimitPatch: AppLimitPatch }) =>
      getClient()
        .appLimit.update({
          where: {
            id,
          },
          data: appLimitPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appLimitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
