/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCapKeys } from '../query-keys';
import { orgLimitCapMutationKeys } from '../mutation-keys';
import type {
  OrgLimitCapSelect,
  OrgLimitCapWithRelations,
  OrgLimitCapPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitCapSelect,
  OrgLimitCapWithRelations,
  OrgLimitCapPatch,
} from '../../orm/input-types';
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgLimitCapMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgLimitCapPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgLimitCapMutation<S extends OrgLimitCapSelect>(
  params: {
    selection: {
      fields: S & OrgLimitCapSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgLimitCap: {
          orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgLimitCapPatch: OrgLimitCapPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgLimitCap: {
      orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgLimitCapPatch: OrgLimitCapPatch;
  }
>;
export function useUpdateOrgLimitCapMutation(
  params: {
    selection: SelectionConfig<OrgLimitCapSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgLimitCapPatch: OrgLimitCapPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitCapMutationKeys.all,
    mutationFn: ({ id, orgLimitCapPatch }: { id: string; orgLimitCapPatch: OrgLimitCapPatch }) =>
      getClient()
        .orgLimitCap.update({
          where: {
            id,
          },
          data: orgLimitCapPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgLimitCapKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
