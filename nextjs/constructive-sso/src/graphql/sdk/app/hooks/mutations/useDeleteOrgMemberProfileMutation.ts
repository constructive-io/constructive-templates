/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMemberProfileKeys } from '../query-keys';
import { orgMemberProfileMutationKeys } from '../mutation-keys';
import type { OrgMemberProfileSelect, OrgMemberProfileWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgMemberProfileSelect, OrgMemberProfileWithRelations } from '../../orm/input-types';
/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgMemberProfileMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgMemberProfileMutation<S extends OrgMemberProfileSelect>(
  params: {
    selection: {
      fields: S & OrgMemberProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgMemberProfile: {
          orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgMemberProfile: {
      orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgMemberProfileMutation(
  params: {
    selection: SelectionConfig<OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMemberProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMemberProfileMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgMemberProfile.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgMemberProfileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMemberProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
