/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appInviteKeys } from '../query-keys';
import { appInviteMutationKeys } from '../mutation-keys';
import type {
  AppInviteSelect,
  AppInviteWithRelations,
  AppInvitePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppInviteSelect,
  AppInviteWithRelations,
  AppInvitePatch,
} from '../../orm/input-types';
/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appInvitePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppInviteMutation<S extends AppInviteSelect>(
  params: {
    selection: {
      fields: S & AppInviteSelect;
    } & HookStrictSelect<NoInfer<S>, AppInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppInvite: {
          appInvite: InferSelectResult<AppInviteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appInvitePatch: AppInvitePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppInvite: {
      appInvite: InferSelectResult<AppInviteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appInvitePatch: AppInvitePatch;
  }
>;
export function useUpdateAppInviteMutation(
  params: {
    selection: SelectionConfig<AppInviteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appInvitePatch: AppInvitePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appInviteMutationKeys.all,
    mutationFn: ({ id, appInvitePatch }: { id: string; appInvitePatch: AppInvitePatch }) =>
      getClient()
        .appInvite.update({
          where: {
            id,
          },
          data: appInvitePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appInviteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
