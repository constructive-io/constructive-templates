/**
 * Tracks per-actor usage counts against configurable maximum limits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitKeys } from '../query-keys';
import { orgLimitMutationKeys } from '../mutation-keys';
import type {
  OrgLimitSelect,
  OrgLimitWithRelations,
  CreateOrgLimitInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitSelect,
  OrgLimitWithRelations,
  CreateOrgLimitInput,
} from '../../orm/input-types';
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgLimitMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgLimitMutation<S extends OrgLimitSelect>(
  params: {
    selection: {
      fields: S & OrgLimitSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgLimit: {
          orgLimit: InferSelectResult<OrgLimitWithRelations, S>;
        };
      },
      Error,
      CreateOrgLimitInput['orgLimit']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgLimit: {
      orgLimit: InferSelectResult<OrgLimitWithRelations, S>;
    };
  },
  Error,
  CreateOrgLimitInput['orgLimit']
>;
export function useCreateOrgLimitMutation(
  params: {
    selection: SelectionConfig<OrgLimitSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgLimitInput['orgLimit']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgLimitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitMutationKeys.create(),
    mutationFn: (data: CreateOrgLimitInput['orgLimit']) =>
      getClient()
        .orgLimit.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgLimitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
