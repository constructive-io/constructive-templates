/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgInviteKeys } from '../query-keys';
import { orgInviteMutationKeys } from '../mutation-keys';
import type {
  OrgInviteSelect,
  OrgInviteWithRelations,
  CreateOrgInviteInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgInviteSelect,
  OrgInviteWithRelations,
  CreateOrgInviteInput,
} from '../../orm/input-types';
/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgInviteMutation<S extends OrgInviteSelect>(
  params: {
    selection: {
      fields: S & OrgInviteSelect;
    } & HookStrictSelect<NoInfer<S>, OrgInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgInvite: {
          orgInvite: InferSelectResult<OrgInviteWithRelations, S>;
        };
      },
      Error,
      CreateOrgInviteInput['orgInvite']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgInvite: {
      orgInvite: InferSelectResult<OrgInviteWithRelations, S>;
    };
  },
  Error,
  CreateOrgInviteInput['orgInvite']
>;
export function useCreateOrgInviteMutation(
  params: {
    selection: SelectionConfig<OrgInviteSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgInviteInput['orgInvite']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgInviteMutationKeys.create(),
    mutationFn: (data: CreateOrgInviteInput['orgInvite']) =>
      getClient()
        .orgInvite.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
