/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMembershipProfileKeys } from '../query-keys';
import { orgMembershipProfileMutationKeys } from '../mutation-keys';
import type {
  OrgMembershipProfileSelect,
  OrgMembershipProfileWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMembershipProfileSelect,
  OrgMembershipProfileWithRelations,
} from '../../orm/input-types';
/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgMembershipProfileMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgMembershipProfileMutation<S extends OrgMembershipProfileSelect>(
  params: {
    selection: {
      fields: S & OrgMembershipProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMembershipProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgMembershipProfile: {
          orgMembershipProfile: InferSelectResult<OrgMembershipProfileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgMembershipProfile: {
      orgMembershipProfile: InferSelectResult<OrgMembershipProfileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgMembershipProfileMutation(
  params: {
    selection: SelectionConfig<OrgMembershipProfileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMembershipProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMembershipProfileMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgMembershipProfile.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgMembershipProfileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMembershipProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
