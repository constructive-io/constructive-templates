/**
 * Records of individual permission grants and revocations for members via bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appGrantKeys } from '../query-keys';
import { appGrantMutationKeys } from '../mutation-keys';
import type { AppGrantSelect, AppGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppGrantSelect, AppGrantWithRelations } from '../../orm/input-types';
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppGrantMutation<S extends AppGrantSelect>(
  params: {
    selection: {
      fields: S & AppGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppGrant: {
          appGrant: InferSelectResult<AppGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppGrant: {
      appGrant: InferSelectResult<AppGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppGrantMutation(
  params: {
    selection: SelectionConfig<AppGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
