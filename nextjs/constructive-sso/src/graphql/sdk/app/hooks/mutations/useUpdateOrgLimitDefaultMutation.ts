/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitDefaultKeys } from '../query-keys';
import { orgLimitDefaultMutationKeys } from '../mutation-keys';
import type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  OrgLimitDefaultPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  OrgLimitDefaultPatch,
} from '../../orm/input-types';
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgLimitDefaultMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgLimitDefaultPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgLimitDefaultMutation<S extends OrgLimitDefaultSelect>(
  params: {
    selection: {
      fields: S & OrgLimitDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgLimitDefault: {
          orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgLimitDefaultPatch: OrgLimitDefaultPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgLimitDefault: {
      orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgLimitDefaultPatch: OrgLimitDefaultPatch;
  }
>;
export function useUpdateOrgLimitDefaultMutation(
  params: {
    selection: SelectionConfig<OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgLimitDefaultPatch: OrgLimitDefaultPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitDefaultMutationKeys.all,
    mutationFn: ({
      id,
      orgLimitDefaultPatch,
    }: {
      id: string;
      orgLimitDefaultPatch: OrgLimitDefaultPatch;
    }) =>
      getClient()
        .orgLimitDefault.update({
          where: {
            id,
          },
          data: orgLimitDefaultPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgLimitDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
