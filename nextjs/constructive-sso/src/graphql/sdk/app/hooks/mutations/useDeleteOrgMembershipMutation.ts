/**
 * Tracks membership records linking actors to entities with permission bitmasks, ownership, and admin status
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMembershipKeys } from '../query-keys';
import { orgMembershipMutationKeys } from '../mutation-keys';
import type { OrgMembershipSelect, OrgMembershipWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgMembershipSelect, OrgMembershipWithRelations } from '../../orm/input-types';
/**
 * Tracks membership records linking actors to entities with permission bitmasks, ownership, and admin status
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgMembershipMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgMembershipMutation<S extends OrgMembershipSelect>(
  params: {
    selection: {
      fields: S & OrgMembershipSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMembershipSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgMembership: {
          orgMembership: InferSelectResult<OrgMembershipWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgMembership: {
      orgMembership: InferSelectResult<OrgMembershipWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgMembershipMutation(
  params: {
    selection: SelectionConfig<OrgMembershipSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMembershipSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMembershipMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgMembership.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgMembershipKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMembershipKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
