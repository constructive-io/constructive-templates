/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appPermissionKeys } from '../query-keys';
import { appPermissionMutationKeys } from '../mutation-keys';
import type {
  AppPermissionSelect,
  AppPermissionWithRelations,
  AppPermissionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppPermissionSelect,
  AppPermissionWithRelations,
  AppPermissionPatch,
} from '../../orm/input-types';
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppPermissionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appPermissionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppPermissionMutation<S extends AppPermissionSelect>(
  params: {
    selection: {
      fields: S & AppPermissionSelect;
    } & HookStrictSelect<NoInfer<S>, AppPermissionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppPermission: {
          appPermission: InferSelectResult<AppPermissionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appPermissionPatch: AppPermissionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppPermission: {
      appPermission: InferSelectResult<AppPermissionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appPermissionPatch: AppPermissionPatch;
  }
>;
export function useUpdateAppPermissionMutation(
  params: {
    selection: SelectionConfig<AppPermissionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appPermissionPatch: AppPermissionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppPermissionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appPermissionMutationKeys.all,
    mutationFn: ({
      id,
      appPermissionPatch,
    }: {
      id: string;
      appPermissionPatch: AppPermissionPatch;
    }) =>
      getClient()
        .appPermission.update({
          where: {
            id,
          },
          data: appPermissionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appPermissionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appPermissionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
