/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appFileKeys } from '../query-keys';
import { appFileMutationKeys } from '../mutation-keys';
import type { AppFileSelect, AppFileWithRelations, AppFilePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppFileSelect, AppFileWithRelations, AppFilePatch } from '../../orm/input-types';
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppFileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appFilePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppFileMutation<S extends AppFileSelect>(
  params: {
    selection: {
      fields: S & AppFileSelect;
    } & HookStrictSelect<NoInfer<S>, AppFileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppFile: {
          appFile: InferSelectResult<AppFileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appFilePatch: AppFilePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppFile: {
      appFile: InferSelectResult<AppFileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appFilePatch: AppFilePatch;
  }
>;
export function useUpdateAppFileMutation(
  params: {
    selection: SelectionConfig<AppFileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appFilePatch: AppFilePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppFileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appFileMutationKeys.all,
    mutationFn: ({ id, appFilePatch }: { id: string; appFilePatch: AppFilePatch }) =>
      getClient()
        .appFile.update({
          where: {
            id,
          },
          data: appFilePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appFileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appFileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
