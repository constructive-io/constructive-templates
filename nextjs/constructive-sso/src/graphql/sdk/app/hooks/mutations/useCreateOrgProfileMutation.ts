/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileKeys } from '../query-keys';
import { orgProfileMutationKeys } from '../mutation-keys';
import type {
  OrgProfileSelect,
  OrgProfileWithRelations,
  CreateOrgProfileInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileSelect,
  OrgProfileWithRelations,
  CreateOrgProfileInput,
} from '../../orm/input-types';
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgProfileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgProfileMutation<S extends OrgProfileSelect>(
  params: {
    selection: {
      fields: S & OrgProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgProfile: {
          orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
        };
      },
      Error,
      CreateOrgProfileInput['orgProfile']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgProfile: {
      orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
    };
  },
  Error,
  CreateOrgProfileInput['orgProfile']
>;
export function useCreateOrgProfileMutation(
  params: {
    selection: SelectionConfig<OrgProfileSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgProfileInput['orgProfile']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileMutationKeys.create(),
    mutationFn: (data: CreateOrgProfileInput['orgProfile']) =>
      getClient()
        .orgProfile.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
