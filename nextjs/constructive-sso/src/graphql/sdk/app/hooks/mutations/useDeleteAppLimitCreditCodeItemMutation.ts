/**
 * Items within a credit code — each row grants credits for a specific limit definition
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCreditCodeItemKeys } from '../query-keys';
import { appLimitCreditCodeItemMutationKeys } from '../mutation-keys';
import type {
  AppLimitCreditCodeItemSelect,
  AppLimitCreditCodeItemWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLimitCreditCodeItemSelect,
  AppLimitCreditCodeItemWithRelations,
} from '../../orm/input-types';
/**
 * Items within a credit code — each row grants credits for a specific limit definition
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLimitCreditCodeItemMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLimitCreditCodeItemMutation<S extends AppLimitCreditCodeItemSelect>(
  params: {
    selection: {
      fields: S & AppLimitCreditCodeItemSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitCreditCodeItemSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLimitCreditCodeItem: {
          appLimitCreditCodeItem: InferSelectResult<AppLimitCreditCodeItemWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLimitCreditCodeItem: {
      appLimitCreditCodeItem: InferSelectResult<AppLimitCreditCodeItemWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLimitCreditCodeItemMutation(
  params: {
    selection: SelectionConfig<AppLimitCreditCodeItemSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitCreditCodeItemSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitCreditCodeItemMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLimitCreditCodeItem.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLimitCreditCodeItemKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitCreditCodeItemKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
