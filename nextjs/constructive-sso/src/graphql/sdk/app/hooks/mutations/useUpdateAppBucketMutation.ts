/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appBucketKeys } from '../query-keys';
import { appBucketMutationKeys } from '../mutation-keys';
import type {
  AppBucketSelect,
  AppBucketWithRelations,
  AppBucketPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppBucketSelect,
  AppBucketWithRelations,
  AppBucketPatch,
} from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appBucketPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppBucketMutation<S extends AppBucketSelect>(
  params: {
    selection: {
      fields: S & AppBucketSelect;
    } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppBucket: {
          appBucket: InferSelectResult<AppBucketWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appBucketPatch: AppBucketPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppBucket: {
      appBucket: InferSelectResult<AppBucketWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appBucketPatch: AppBucketPatch;
  }
>;
export function useUpdateAppBucketMutation(
  params: {
    selection: SelectionConfig<AppBucketSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appBucketPatch: AppBucketPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appBucketMutationKeys.all,
    mutationFn: ({ id, appBucketPatch }: { id: string; appBucketPatch: AppBucketPatch }) =>
      getClient()
        .appBucket.update({
          where: {
            id,
          },
          data: appBucketPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appBucketKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appBucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
