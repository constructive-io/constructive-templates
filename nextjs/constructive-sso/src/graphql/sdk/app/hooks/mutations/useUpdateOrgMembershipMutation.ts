/**
 * Tracks membership records linking actors to entities with permission bitmasks, ownership, and admin status
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMembershipKeys } from '../query-keys';
import { orgMembershipMutationKeys } from '../mutation-keys';
import type {
  OrgMembershipSelect,
  OrgMembershipWithRelations,
  OrgMembershipPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMembershipSelect,
  OrgMembershipWithRelations,
  OrgMembershipPatch,
} from '../../orm/input-types';
/**
 * Tracks membership records linking actors to entities with permission bitmasks, ownership, and admin status
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgMembershipMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgMembershipPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgMembershipMutation<S extends OrgMembershipSelect>(
  params: {
    selection: {
      fields: S & OrgMembershipSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMembershipSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgMembership: {
          orgMembership: InferSelectResult<OrgMembershipWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgMembershipPatch: OrgMembershipPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgMembership: {
      orgMembership: InferSelectResult<OrgMembershipWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgMembershipPatch: OrgMembershipPatch;
  }
>;
export function useUpdateOrgMembershipMutation(
  params: {
    selection: SelectionConfig<OrgMembershipSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgMembershipPatch: OrgMembershipPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMembershipSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMembershipMutationKeys.all,
    mutationFn: ({
      id,
      orgMembershipPatch,
    }: {
      id: string;
      orgMembershipPatch: OrgMembershipPatch;
    }) =>
      getClient()
        .orgMembership.update({
          where: {
            id,
          },
          data: orgMembershipPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgMembershipKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMembershipKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
