/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMemberProfileKeys } from '../query-keys';
import { orgMemberProfileMutationKeys } from '../mutation-keys';
import type {
  OrgMemberProfileSelect,
  OrgMemberProfileWithRelations,
  OrgMemberProfilePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMemberProfileSelect,
  OrgMemberProfileWithRelations,
  OrgMemberProfilePatch,
} from '../../orm/input-types';
/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgMemberProfileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgMemberProfilePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgMemberProfileMutation<S extends OrgMemberProfileSelect>(
  params: {
    selection: {
      fields: S & OrgMemberProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgMemberProfile: {
          orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgMemberProfilePatch: OrgMemberProfilePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgMemberProfile: {
      orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgMemberProfilePatch: OrgMemberProfilePatch;
  }
>;
export function useUpdateOrgMemberProfileMutation(
  params: {
    selection: SelectionConfig<OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgMemberProfilePatch: OrgMemberProfilePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMemberProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMemberProfileMutationKeys.all,
    mutationFn: ({
      id,
      orgMemberProfilePatch,
    }: {
      id: string;
      orgMemberProfilePatch: OrgMemberProfilePatch;
    }) =>
      getClient()
        .orgMemberProfile.update({
          where: {
            id,
          },
          data: orgMemberProfilePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgMemberProfileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMemberProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
