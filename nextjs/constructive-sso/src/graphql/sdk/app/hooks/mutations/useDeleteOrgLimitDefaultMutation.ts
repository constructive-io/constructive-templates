/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitDefaultKeys } from '../query-keys';
import { orgLimitDefaultMutationKeys } from '../mutation-keys';
import type { OrgLimitDefaultSelect, OrgLimitDefaultWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitDefaultSelect, OrgLimitDefaultWithRelations } from '../../orm/input-types';
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgLimitDefaultMutation<S extends OrgLimitDefaultSelect>(
  params: {
    selection: {
      fields: S & OrgLimitDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgLimitDefault: {
          orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgLimitDefault: {
      orgLimitDefault: InferSelectResult<OrgLimitDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgLimitDefaultMutation(
  params: {
    selection: SelectionConfig<OrgLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgLimitDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgLimitDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
