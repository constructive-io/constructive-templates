/**
 * Records of individual permission grants and revocations for members via bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgGrantKeys } from '../query-keys';
import { orgGrantMutationKeys } from '../mutation-keys';
import type { OrgGrantSelect, OrgGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgGrantSelect, OrgGrantWithRelations } from '../../orm/input-types';
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgGrantMutation<S extends OrgGrantSelect>(
  params: {
    selection: {
      fields: S & OrgGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgGrant: {
          orgGrant: InferSelectResult<OrgGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgGrant: {
      orgGrant: InferSelectResult<OrgGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgGrantMutation(
  params: {
    selection: SelectionConfig<OrgGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
