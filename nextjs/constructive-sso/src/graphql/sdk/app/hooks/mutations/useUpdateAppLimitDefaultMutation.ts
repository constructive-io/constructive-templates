/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitDefaultKeys } from '../query-keys';
import { appLimitDefaultMutationKeys } from '../mutation-keys';
import type {
  AppLimitDefaultSelect,
  AppLimitDefaultWithRelations,
  AppLimitDefaultPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLimitDefaultSelect,
  AppLimitDefaultWithRelations,
  AppLimitDefaultPatch,
} from '../../orm/input-types';
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLimitDefaultMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appLimitDefaultPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppLimitDefaultMutation<S extends AppLimitDefaultSelect>(
  params: {
    selection: {
      fields: S & AppLimitDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppLimitDefault: {
          appLimitDefault: InferSelectResult<AppLimitDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appLimitDefaultPatch: AppLimitDefaultPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppLimitDefault: {
      appLimitDefault: InferSelectResult<AppLimitDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appLimitDefaultPatch: AppLimitDefaultPatch;
  }
>;
export function useUpdateAppLimitDefaultMutation(
  params: {
    selection: SelectionConfig<AppLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appLimitDefaultPatch: AppLimitDefaultPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitDefaultMutationKeys.all,
    mutationFn: ({
      id,
      appLimitDefaultPatch,
    }: {
      id: string;
      appLimitDefaultPatch: AppLimitDefaultPatch;
    }) =>
      getClient()
        .appLimitDefault.update({
          where: {
            id,
          },
          data: appLimitDefaultPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appLimitDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
