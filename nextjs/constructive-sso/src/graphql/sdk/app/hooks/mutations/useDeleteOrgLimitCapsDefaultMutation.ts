/**
 * Default cap values for static configuration limits (max file size, feature flags, etc.). Not metered — just read by consumers.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCapsDefaultKeys } from '../query-keys';
import { orgLimitCapsDefaultMutationKeys } from '../mutation-keys';
import type {
  OrgLimitCapsDefaultSelect,
  OrgLimitCapsDefaultWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgLimitCapsDefaultSelect,
  OrgLimitCapsDefaultWithRelations,
} from '../../orm/input-types';
/**
 * Default cap values for static configuration limits (max file size, feature flags, etc.). Not metered — just read by consumers.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgLimitCapsDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgLimitCapsDefaultMutation<S extends OrgLimitCapsDefaultSelect>(
  params: {
    selection: {
      fields: S & OrgLimitCapsDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCapsDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgLimitCapsDefault: {
          orgLimitCapsDefault: InferSelectResult<OrgLimitCapsDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgLimitCapsDefault: {
      orgLimitCapsDefault: InferSelectResult<OrgLimitCapsDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgLimitCapsDefaultMutation(
  params: {
    selection: SelectionConfig<OrgLimitCapsDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgLimitCapsDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgLimitCapsDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgLimitCapsDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgLimitCapsDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgLimitCapsDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
