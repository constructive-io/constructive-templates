/**
 * Records of ownership transfers and grants between members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appOwnerGrantKeys } from '../query-keys';
import { appOwnerGrantMutationKeys } from '../mutation-keys';
import type {
  AppOwnerGrantSelect,
  AppOwnerGrantWithRelations,
  AppOwnerGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppOwnerGrantSelect,
  AppOwnerGrantWithRelations,
  AppOwnerGrantPatch,
} from '../../orm/input-types';
/**
 * Records of ownership transfers and grants between members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppOwnerGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appOwnerGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppOwnerGrantMutation<S extends AppOwnerGrantSelect>(
  params: {
    selection: {
      fields: S & AppOwnerGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppOwnerGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppOwnerGrant: {
          appOwnerGrant: InferSelectResult<AppOwnerGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appOwnerGrantPatch: AppOwnerGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppOwnerGrant: {
      appOwnerGrant: InferSelectResult<AppOwnerGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appOwnerGrantPatch: AppOwnerGrantPatch;
  }
>;
export function useUpdateAppOwnerGrantMutation(
  params: {
    selection: SelectionConfig<AppOwnerGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appOwnerGrantPatch: AppOwnerGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppOwnerGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appOwnerGrantMutationKeys.all,
    mutationFn: ({
      id,
      appOwnerGrantPatch,
    }: {
      id: string;
      appOwnerGrantPatch: AppOwnerGrantPatch;
    }) =>
      getClient()
        .appOwnerGrant.update({
          where: {
            id,
          },
          data: appOwnerGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appOwnerGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appOwnerGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
