/**
 * Stores the default permission bitmask assigned to new members upon joining
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgPermissionDefaultKeys } from '../query-keys';
import { orgPermissionDefaultMutationKeys } from '../mutation-keys';
import type {
  OrgPermissionDefaultSelect,
  OrgPermissionDefaultWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgPermissionDefaultSelect,
  OrgPermissionDefaultWithRelations,
} from '../../orm/input-types';
/**
 * Stores the default permission bitmask assigned to new members upon joining
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgPermissionDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgPermissionDefaultMutation<S extends OrgPermissionDefaultSelect>(
  params: {
    selection: {
      fields: S & OrgPermissionDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgPermissionDefault: {
          orgPermissionDefault: InferSelectResult<OrgPermissionDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgPermissionDefault: {
      orgPermissionDefault: InferSelectResult<OrgPermissionDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgPermissionDefaultMutation(
  params: {
    selection: SelectionConfig<OrgPermissionDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgPermissionDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgPermissionDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgPermissionDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgPermissionDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgPermissionDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
