/**
 * User phone numbers with country code, verification, and primary-number management
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { phoneNumberKeys } from '../query-keys';
import { phoneNumberMutationKeys } from '../mutation-keys';
import type {
  PhoneNumberSelect,
  PhoneNumberWithRelations,
  CreatePhoneNumberInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PhoneNumberSelect,
  PhoneNumberWithRelations,
  CreatePhoneNumberInput,
} from '../../orm/input-types';
/**
 * User phone numbers with country code, verification, and primary-number management
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePhoneNumberMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePhoneNumberMutation<S extends PhoneNumberSelect>(
  params: {
    selection: {
      fields: S & PhoneNumberSelect;
    } & HookStrictSelect<NoInfer<S>, PhoneNumberSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPhoneNumber: {
          phoneNumber: InferSelectResult<PhoneNumberWithRelations, S>;
        };
      },
      Error,
      CreatePhoneNumberInput['phoneNumber']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPhoneNumber: {
      phoneNumber: InferSelectResult<PhoneNumberWithRelations, S>;
    };
  },
  Error,
  CreatePhoneNumberInput['phoneNumber']
>;
export function useCreatePhoneNumberMutation(
  params: {
    selection: SelectionConfig<PhoneNumberSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePhoneNumberInput['phoneNumber']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PhoneNumberSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: phoneNumberMutationKeys.create(),
    mutationFn: (data: CreatePhoneNumberInput['phoneNumber']) =>
      getClient()
        .phoneNumber.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: phoneNumberKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
