/**
 * Association table scoping principals to specific organizations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { principalEntityKeys } from '../query-keys';
import { principalEntityMutationKeys } from '../mutation-keys';
import type { PrincipalEntitySelect, PrincipalEntityWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PrincipalEntitySelect, PrincipalEntityWithRelations } from '../../orm/input-types';
/**
 * Association table scoping principals to specific organizations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePrincipalEntityMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePrincipalEntityMutation<S extends PrincipalEntitySelect>(
  params: {
    selection: {
      fields: S & PrincipalEntitySelect;
    } & HookStrictSelect<NoInfer<S>, PrincipalEntitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePrincipalEntity: {
          principalEntity: InferSelectResult<PrincipalEntityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePrincipalEntity: {
      principalEntity: InferSelectResult<PrincipalEntityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePrincipalEntityMutation(
  params: {
    selection: SelectionConfig<PrincipalEntitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PrincipalEntitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: principalEntityMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .principalEntity.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: principalEntityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: principalEntityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
