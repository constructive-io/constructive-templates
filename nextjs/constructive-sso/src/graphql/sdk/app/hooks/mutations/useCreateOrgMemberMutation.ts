/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMemberKeys } from '../query-keys';
import { orgMemberMutationKeys } from '../mutation-keys';
import type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  CreateOrgMemberInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  CreateOrgMemberInput,
} from '../../orm/input-types';
/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgMemberMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgMemberMutation<S extends OrgMemberSelect>(
  params: {
    selection: {
      fields: S & OrgMemberSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMemberSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgMember: {
          orgMember: InferSelectResult<OrgMemberWithRelations, S>;
        };
      },
      Error,
      CreateOrgMemberInput['orgMember']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgMember: {
      orgMember: InferSelectResult<OrgMemberWithRelations, S>;
    };
  },
  Error,
  CreateOrgMemberInput['orgMember']
>;
export function useCreateOrgMemberMutation(
  params: {
    selection: SelectionConfig<OrgMemberSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgMemberInput['orgMember']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgMemberSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMemberMutationKeys.create(),
    mutationFn: (data: CreateOrgMemberInput['orgMember']) =>
      getClient()
        .orgMember.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgMemberKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
