/**
 * Update mutation hook for User
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { userKeys } from '../query-keys';
import { userMutationKeys } from '../mutation-keys';
import type { UserSelect, UserWithRelations, UserPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { UserSelect, UserWithRelations, UserPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a User
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateUserMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', userPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateUserMutation<S extends UserSelect>(
  params: {
    selection: {
      fields: S & UserSelect;
    } & HookStrictSelect<NoInfer<S>, UserSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateUser: {
          user: InferSelectResult<UserWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        userPatch: UserPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateUser: {
      user: InferSelectResult<UserWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    userPatch: UserPatch;
  }
>;
export function useUpdateUserMutation(
  params: {
    selection: SelectionConfig<UserSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        userPatch: UserPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<UserSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: userMutationKeys.all,
    mutationFn: ({ id, userPatch }: { id: string; userPatch: UserPatch }) =>
      getClient()
        .user.update({
          where: {
            id,
          },
          data: userPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: userKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: userKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
