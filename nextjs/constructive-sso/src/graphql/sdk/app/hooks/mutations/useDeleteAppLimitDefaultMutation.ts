/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitDefaultKeys } from '../query-keys';
import { appLimitDefaultMutationKeys } from '../mutation-keys';
import type { AppLimitDefaultSelect, AppLimitDefaultWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLimitDefaultSelect, AppLimitDefaultWithRelations } from '../../orm/input-types';
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLimitDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLimitDefaultMutation<S extends AppLimitDefaultSelect>(
  params: {
    selection: {
      fields: S & AppLimitDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, AppLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLimitDefault: {
          appLimitDefault: InferSelectResult<AppLimitDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLimitDefault: {
      appLimitDefault: InferSelectResult<AppLimitDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLimitDefaultMutation(
  params: {
    selection: SelectionConfig<AppLimitDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLimitDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLimitDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLimitDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLimitDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLimitDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
