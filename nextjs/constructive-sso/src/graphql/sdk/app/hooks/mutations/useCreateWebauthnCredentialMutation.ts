/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webauthnCredentialKeys } from '../query-keys';
import { webauthnCredentialMutationKeys } from '../mutation-keys';
import type {
  WebauthnCredentialSelect,
  WebauthnCredentialWithRelations,
  CreateWebauthnCredentialInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  WebauthnCredentialSelect,
  WebauthnCredentialWithRelations,
  CreateWebauthnCredentialInput,
} from '../../orm/input-types';
/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateWebauthnCredentialMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateWebauthnCredentialMutation<S extends WebauthnCredentialSelect>(
  params: {
    selection: {
      fields: S & WebauthnCredentialSelect;
    } & HookStrictSelect<NoInfer<S>, WebauthnCredentialSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createWebauthnCredential: {
          webauthnCredential: InferSelectResult<WebauthnCredentialWithRelations, S>;
        };
      },
      Error,
      CreateWebauthnCredentialInput['webauthnCredential']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createWebauthnCredential: {
      webauthnCredential: InferSelectResult<WebauthnCredentialWithRelations, S>;
    };
  },
  Error,
  CreateWebauthnCredentialInput['webauthnCredential']
>;
export function useCreateWebauthnCredentialMutation(
  params: {
    selection: SelectionConfig<WebauthnCredentialSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateWebauthnCredentialInput['webauthnCredential']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<WebauthnCredentialSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: webauthnCredentialMutationKeys.create(),
    mutationFn: (data: CreateWebauthnCredentialInput['webauthnCredential']) =>
      getClient()
        .webauthnCredential.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: webauthnCredentialKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
