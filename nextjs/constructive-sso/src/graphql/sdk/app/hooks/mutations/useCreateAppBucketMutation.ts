/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appBucketKeys } from '../query-keys';
import { appBucketMutationKeys } from '../mutation-keys';
import type {
  AppBucketSelect,
  AppBucketWithRelations,
  CreateAppBucketInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppBucketSelect,
  AppBucketWithRelations,
  CreateAppBucketInput,
} from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppBucketMutation<S extends AppBucketSelect>(
  params: {
    selection: {
      fields: S & AppBucketSelect;
    } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppBucket: {
          appBucket: InferSelectResult<AppBucketWithRelations, S>;
        };
      },
      Error,
      CreateAppBucketInput['appBucket']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppBucket: {
      appBucket: InferSelectResult<AppBucketWithRelations, S>;
    };
  },
  Error,
  CreateAppBucketInput['appBucket']
>;
export function useCreateAppBucketMutation(
  params: {
    selection: SelectionConfig<AppBucketSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppBucketInput['appBucket']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appBucketMutationKeys.create(),
    mutationFn: (data: CreateAppBucketInput['appBucket']) =>
      getClient()
        .appBucket.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appBucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
