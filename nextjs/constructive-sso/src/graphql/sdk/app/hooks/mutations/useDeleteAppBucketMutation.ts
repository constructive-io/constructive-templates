/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appBucketKeys } from '../query-keys';
import { appBucketMutationKeys } from '../mutation-keys';
import type { AppBucketSelect, AppBucketWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppBucketSelect, AppBucketWithRelations } from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppBucketMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppBucketMutation<S extends AppBucketSelect>(
  params: {
    selection: {
      fields: S & AppBucketSelect;
    } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppBucket: {
          appBucket: InferSelectResult<AppBucketWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppBucket: {
      appBucket: InferSelectResult<AppBucketWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppBucketMutation(
  params: {
    selection: SelectionConfig<AppBucketSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appBucketMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appBucket.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appBucketKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appBucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
