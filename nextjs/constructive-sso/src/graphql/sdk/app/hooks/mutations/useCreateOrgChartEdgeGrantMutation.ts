/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgChartEdgeGrantKeys } from '../query-keys';
import { orgChartEdgeGrantMutationKeys } from '../mutation-keys';
import type {
  OrgChartEdgeGrantSelect,
  OrgChartEdgeGrantWithRelations,
  CreateOrgChartEdgeGrantInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgChartEdgeGrantSelect,
  OrgChartEdgeGrantWithRelations,
  CreateOrgChartEdgeGrantInput,
} from '../../orm/input-types';
/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgChartEdgeGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgChartEdgeGrantMutation<S extends OrgChartEdgeGrantSelect>(
  params: {
    selection: {
      fields: S & OrgChartEdgeGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgChartEdgeGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgChartEdgeGrant: {
          orgChartEdgeGrant: InferSelectResult<OrgChartEdgeGrantWithRelations, S>;
        };
      },
      Error,
      CreateOrgChartEdgeGrantInput['orgChartEdgeGrant']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgChartEdgeGrant: {
      orgChartEdgeGrant: InferSelectResult<OrgChartEdgeGrantWithRelations, S>;
    };
  },
  Error,
  CreateOrgChartEdgeGrantInput['orgChartEdgeGrant']
>;
export function useCreateOrgChartEdgeGrantMutation(
  params: {
    selection: SelectionConfig<OrgChartEdgeGrantSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOrgChartEdgeGrantInput['orgChartEdgeGrant']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgChartEdgeGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgChartEdgeGrantMutationKeys.create(),
    mutationFn: (data: CreateOrgChartEdgeGrantInput['orgChartEdgeGrant']) =>
      getClient()
        .orgChartEdgeGrant.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgChartEdgeGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
