/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appInviteKeys } from '../query-keys';
import { appInviteMutationKeys } from '../mutation-keys';
import type {
  AppInviteSelect,
  AppInviteWithRelations,
  CreateAppInviteInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppInviteSelect,
  AppInviteWithRelations,
  CreateAppInviteInput,
} from '../../orm/input-types';
/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppInviteMutation<S extends AppInviteSelect>(
  params: {
    selection: {
      fields: S & AppInviteSelect;
    } & HookStrictSelect<NoInfer<S>, AppInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppInvite: {
          appInvite: InferSelectResult<AppInviteWithRelations, S>;
        };
      },
      Error,
      CreateAppInviteInput['appInvite']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppInvite: {
      appInvite: InferSelectResult<AppInviteWithRelations, S>;
    };
  },
  Error,
  CreateAppInviteInput['appInvite']
>;
export function useCreateAppInviteMutation(
  params: {
    selection: SelectionConfig<AppInviteSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppInviteInput['appInvite']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appInviteMutationKeys.create(),
    mutationFn: (data: CreateAppInviteInput['appInvite']) =>
      getClient()
        .appInvite.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
