/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileGrantKeys } from '../query-keys';
import { orgProfileGrantMutationKeys } from '../mutation-keys';
import type {
  OrgProfileGrantSelect,
  OrgProfileGrantWithRelations,
  CreateOrgProfileGrantInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileGrantSelect,
  OrgProfileGrantWithRelations,
  CreateOrgProfileGrantInput,
} from '../../orm/input-types';
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgProfileGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgProfileGrantMutation<S extends OrgProfileGrantSelect>(
  params: {
    selection: {
      fields: S & OrgProfileGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgProfileGrant: {
          orgProfileGrant: InferSelectResult<OrgProfileGrantWithRelations, S>;
        };
      },
      Error,
      CreateOrgProfileGrantInput['orgProfileGrant']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgProfileGrant: {
      orgProfileGrant: InferSelectResult<OrgProfileGrantWithRelations, S>;
    };
  },
  Error,
  CreateOrgProfileGrantInput['orgProfileGrant']
>;
export function useCreateOrgProfileGrantMutation(
  params: {
    selection: SelectionConfig<OrgProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOrgProfileGrantInput['orgProfileGrant']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileGrantMutationKeys.create(),
    mutationFn: (data: CreateOrgProfileGrantInput['orgProfileGrant']) =>
      getClient()
        .orgProfileGrant.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgProfileGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
