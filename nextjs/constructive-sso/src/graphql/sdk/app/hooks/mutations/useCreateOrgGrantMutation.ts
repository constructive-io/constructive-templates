/**
 * Records of individual permission grants and revocations for members via bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgGrantKeys } from '../query-keys';
import { orgGrantMutationKeys } from '../mutation-keys';
import type {
  OrgGrantSelect,
  OrgGrantWithRelations,
  CreateOrgGrantInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgGrantSelect,
  OrgGrantWithRelations,
  CreateOrgGrantInput,
} from '../../orm/input-types';
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgGrantMutation<S extends OrgGrantSelect>(
  params: {
    selection: {
      fields: S & OrgGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgGrant: {
          orgGrant: InferSelectResult<OrgGrantWithRelations, S>;
        };
      },
      Error,
      CreateOrgGrantInput['orgGrant']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgGrant: {
      orgGrant: InferSelectResult<OrgGrantWithRelations, S>;
    };
  },
  Error,
  CreateOrgGrantInput['orgGrant']
>;
export function useCreateOrgGrantMutation(
  params: {
    selection: SelectionConfig<OrgGrantSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgGrantInput['orgGrant']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgGrantMutationKeys.create(),
    mutationFn: (data: CreateOrgGrantInput['orgGrant']) =>
      getClient()
        .orgGrant.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
