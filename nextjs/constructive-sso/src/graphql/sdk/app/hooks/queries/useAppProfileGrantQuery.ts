/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileGrantKeys } from '../query-keys';
import type { AppProfileGrantSelect, AppProfileGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppProfileGrantSelect, AppProfileGrantWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfileGrantQueryKey = appProfileGrantKeys.detail;
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfileGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppProfileGrantQuery<
  S extends AppProfileGrantSelect,
  TData = {
    appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfileGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppProfileGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppProfileGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfileGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * const data = await fetchAppProfileGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppProfileGrantQuery<S extends AppProfileGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
}): Promise<{
  appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S> | null;
}>;
export async function fetchAppProfileGrantQuery(params: {
  id: string;
  selection: SelectionConfig<AppProfileGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppProfileGrantSelect>(params.selection);
  return getClient()
    .appProfileGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * await prefetchAppProfileGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppProfileGrantQuery<S extends AppProfileGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  }
): Promise<void>;
export async function prefetchAppProfileGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppProfileGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppProfileGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfileGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
