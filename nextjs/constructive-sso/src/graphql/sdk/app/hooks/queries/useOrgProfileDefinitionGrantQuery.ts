/**
 * Audit log of permission additions and removals from profile definitions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileDefinitionGrantKeys } from '../query-keys';
import type {
  OrgProfileDefinitionGrantSelect,
  OrgProfileDefinitionGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileDefinitionGrantSelect,
  OrgProfileDefinitionGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgProfileDefinitionGrantQueryKey = orgProfileDefinitionGrantKeys.detail;
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgProfileDefinitionGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgProfileDefinitionGrantQuery<
  S extends OrgProfileDefinitionGrantSelect,
  TData = {
    orgProfileDefinitionGrant: InferSelectResult<OrgProfileDefinitionGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileDefinitionGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgProfileDefinitionGrant: InferSelectResult<
          OrgProfileDefinitionGrantWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgProfileDefinitionGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileDefinitionGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgProfileDefinitionGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgProfileDefinitionGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileDefinitionGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```ts
 * const data = await fetchOrgProfileDefinitionGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgProfileDefinitionGrantQuery<
  S extends OrgProfileDefinitionGrantSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgProfileDefinitionGrantSelect>;
}): Promise<{
  orgProfileDefinitionGrant: InferSelectResult<OrgProfileDefinitionGrantWithRelations, S> | null;
}>;
export async function fetchOrgProfileDefinitionGrantQuery(params: {
  id: string;
  selection: SelectionConfig<OrgProfileDefinitionGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgProfileDefinitionGrantSelect>(params.selection);
  return getClient()
    .orgProfileDefinitionGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```ts
 * await prefetchOrgProfileDefinitionGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgProfileDefinitionGrantQuery<
  S extends OrgProfileDefinitionGrantSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileDefinitionGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgProfileDefinitionGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileDefinitionGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgProfileDefinitionGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgProfileDefinitionGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileDefinitionGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
