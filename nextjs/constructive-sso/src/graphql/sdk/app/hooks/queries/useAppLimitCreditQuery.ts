/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCreditKeys } from '../query-keys';
import type { AppLimitCreditSelect, AppLimitCreditWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLimitCreditSelect, AppLimitCreditWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appLimitCreditQueryKey = appLimitCreditKeys.detail;
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLimitCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppLimitCreditQuery<
  S extends AppLimitCreditSelect,
  TData = {
    appLimitCredit: InferSelectResult<AppLimitCreditWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLimitCreditSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appLimitCredit: InferSelectResult<AppLimitCreditWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppLimitCreditQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppLimitCreditSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppLimitCreditSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appLimitCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLimitCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```ts
 * const data = await fetchAppLimitCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppLimitCreditQuery<S extends AppLimitCreditSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppLimitCreditSelect>;
}): Promise<{
  appLimitCredit: InferSelectResult<AppLimitCreditWithRelations, S> | null;
}>;
export async function fetchAppLimitCreditQuery(params: {
  id: string;
  selection: SelectionConfig<AppLimitCreditSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppLimitCreditSelect>(params.selection);
  return getClient()
    .appLimitCredit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```ts
 * await prefetchAppLimitCreditQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppLimitCreditQuery<S extends AppLimitCreditSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLimitCreditSelect>;
  }
): Promise<void>;
export async function prefetchAppLimitCreditQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppLimitCreditSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppLimitCreditSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appLimitCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLimitCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
