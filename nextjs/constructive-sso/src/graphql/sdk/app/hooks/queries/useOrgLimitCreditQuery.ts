/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCreditKeys } from '../query-keys';
import type { OrgLimitCreditSelect, OrgLimitCreditWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitCreditSelect, OrgLimitCreditWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgLimitCreditQueryKey = orgLimitCreditKeys.detail;
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgLimitCreditQuery<
  S extends OrgLimitCreditSelect,
  TData = {
    orgLimitCredit: InferSelectResult<OrgLimitCreditWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCreditSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgLimitCredit: InferSelectResult<OrgLimitCreditWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgLimitCreditQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitCreditSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgLimitCreditSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgLimitCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimitCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```ts
 * const data = await fetchOrgLimitCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgLimitCreditQuery<S extends OrgLimitCreditSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgLimitCreditSelect>;
}): Promise<{
  orgLimitCredit: InferSelectResult<OrgLimitCreditWithRelations, S> | null;
}>;
export async function fetchOrgLimitCreditQuery(params: {
  id: string;
  selection: SelectionConfig<OrgLimitCreditSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgLimitCreditSelect>(params.selection);
  return getClient()
    .orgLimitCredit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only ledger of credit grants that automatically update limit ceilings
 *
 * @example
 * ```ts
 * await prefetchOrgLimitCreditQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgLimitCreditQuery<S extends OrgLimitCreditSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCreditSelect>;
  }
): Promise<void>;
export async function prefetchOrgLimitCreditQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitCreditSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgLimitCreditSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgLimitCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimitCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
