/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { orgLimitDefaultKeys } from '../query-keys';
import type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  OrgLimitDefaultFilter,
  OrgLimitDefaultOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OrgLimitDefaultSelect,
  OrgLimitDefaultWithRelations,
  OrgLimitDefaultFilter,
  OrgLimitDefaultOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgLimitDefaultsQueryKey = orgLimitDefaultKeys.list;
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitDefaultsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOrgLimitDefaultsQuery<
  S extends OrgLimitDefaultSelect,
  TData = {
    orgLimitDefaults: ConnectionResult<InferSelectResult<OrgLimitDefaultWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgLimitDefaultFilter, OrgLimitDefaultOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgLimitDefaults: ConnectionResult<InferSelectResult<OrgLimitDefaultWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgLimitDefaultsQuery(
  params: {
    selection: ListSelectionConfig<
      OrgLimitDefaultSelect,
      OrgLimitDefaultFilter,
      OrgLimitDefaultOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    OrgLimitDefaultSelect,
    OrgLimitDefaultFilter,
    OrgLimitDefaultOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgLimitDefaultKeys.list(args),
    queryFn: () => getClient().orgLimitDefault.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```ts
 * const data = await fetchOrgLimitDefaultsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOrgLimitDefaultsQuery<S extends OrgLimitDefaultSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, OrgLimitDefaultFilter, OrgLimitDefaultOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
}): Promise<{
  orgLimitDefaults: ConnectionResult<InferSelectResult<OrgLimitDefaultWithRelations, S>>;
}>;
export async function fetchOrgLimitDefaultsQuery(params: {
  selection: ListSelectionConfig<
    OrgLimitDefaultSelect,
    OrgLimitDefaultFilter,
    OrgLimitDefaultOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    OrgLimitDefaultSelect,
    OrgLimitDefaultFilter,
    OrgLimitDefaultOrderBy
  >(params.selection);
  return getClient().orgLimitDefault.findMany(args).unwrap();
}
/**
 * Default maximum values for each named limit, applied when no per-actor override exists
 *
 * @example
 * ```ts
 * await prefetchOrgLimitDefaultsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOrgLimitDefaultsQuery<S extends OrgLimitDefaultSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgLimitDefaultFilter, OrgLimitDefaultOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgLimitDefaultSelect>;
  }
): Promise<void>;
export async function prefetchOrgLimitDefaultsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      OrgLimitDefaultSelect,
      OrgLimitDefaultFilter,
      OrgLimitDefaultOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    OrgLimitDefaultSelect,
    OrgLimitDefaultFilter,
    OrgLimitDefaultOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgLimitDefaultKeys.list(args),
    queryFn: () => getClient().orgLimitDefault.findMany(args).unwrap(),
  });
}
