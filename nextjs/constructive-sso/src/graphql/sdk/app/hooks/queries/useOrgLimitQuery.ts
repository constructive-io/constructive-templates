/**
 * Tracks per-actor usage counts against configurable maximum limits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitKeys } from '../query-keys';
import type { OrgLimitSelect, OrgLimitWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitSelect, OrgLimitWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgLimitQueryKey = orgLimitKeys.detail;
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgLimitQuery<
  S extends OrgLimitSelect,
  TData = {
    orgLimit: InferSelectResult<OrgLimitWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgLimit: InferSelectResult<OrgLimitWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgLimitQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgLimitSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgLimitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```ts
 * const data = await fetchOrgLimitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgLimitQuery<S extends OrgLimitSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgLimitSelect>;
}): Promise<{
  orgLimit: InferSelectResult<OrgLimitWithRelations, S> | null;
}>;
export async function fetchOrgLimitQuery(params: {
  id: string;
  selection: SelectionConfig<OrgLimitSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgLimitSelect>(params.selection);
  return getClient()
    .orgLimit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Tracks per-actor usage counts against configurable maximum limits
 *
 * @example
 * ```ts
 * await prefetchOrgLimitQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgLimitQuery<S extends OrgLimitSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitSelect>;
  }
): Promise<void>;
export async function prefetchOrgLimitQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgLimitSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgLimitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
