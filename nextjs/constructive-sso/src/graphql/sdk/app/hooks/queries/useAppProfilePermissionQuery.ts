/**
 * Join table linking profiles to individual permissions they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfilePermissionKeys } from '../query-keys';
import type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfilePermissionQueryKey = appProfilePermissionKeys.detail;
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfilePermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppProfilePermissionQuery<
  S extends AppProfilePermissionSelect,
  TData = {
    appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfilePermissionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfilePermissionQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppProfilePermissionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppProfilePermissionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfilePermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfilePermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```ts
 * const data = await fetchAppProfilePermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppProfilePermissionQuery<S extends AppProfilePermissionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppProfilePermissionSelect>;
}): Promise<{
  appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S> | null;
}>;
export async function fetchAppProfilePermissionQuery(params: {
  id: string;
  selection: SelectionConfig<AppProfilePermissionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppProfilePermissionSelect>(params.selection);
  return getClient()
    .appProfilePermission.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```ts
 * await prefetchAppProfilePermissionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppProfilePermissionQuery<S extends AppProfilePermissionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfilePermissionSelect>;
  }
): Promise<void>;
export async function prefetchAppProfilePermissionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppProfilePermissionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppProfilePermissionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfilePermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfilePermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
