/**
 * Append-only ledger of code redemptions; AFTER INSERT trigger validates and cascades to limit_credits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLimitCreditRedemptionKeys } from '../query-keys';
import type {
  AppLimitCreditRedemptionSelect,
  AppLimitCreditRedemptionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLimitCreditRedemptionSelect,
  AppLimitCreditRedemptionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appLimitCreditRedemptionQueryKey = appLimitCreditRedemptionKeys.detail;
/**
 * Append-only ledger of code redemptions; AFTER INSERT trigger validates and cascades to limit_credits
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLimitCreditRedemptionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppLimitCreditRedemptionQuery<
  S extends AppLimitCreditRedemptionSelect,
  TData = {
    appLimitCreditRedemption: InferSelectResult<AppLimitCreditRedemptionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLimitCreditRedemptionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appLimitCreditRedemption: InferSelectResult<
          AppLimitCreditRedemptionWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppLimitCreditRedemptionQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppLimitCreditRedemptionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppLimitCreditRedemptionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appLimitCreditRedemptionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLimitCreditRedemption.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only ledger of code redemptions; AFTER INSERT trigger validates and cascades to limit_credits
 *
 * @example
 * ```ts
 * const data = await fetchAppLimitCreditRedemptionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppLimitCreditRedemptionQuery<
  S extends AppLimitCreditRedemptionSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppLimitCreditRedemptionSelect>;
}): Promise<{
  appLimitCreditRedemption: InferSelectResult<AppLimitCreditRedemptionWithRelations, S> | null;
}>;
export async function fetchAppLimitCreditRedemptionQuery(params: {
  id: string;
  selection: SelectionConfig<AppLimitCreditRedemptionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppLimitCreditRedemptionSelect>(params.selection);
  return getClient()
    .appLimitCreditRedemption.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only ledger of code redemptions; AFTER INSERT trigger validates and cascades to limit_credits
 *
 * @example
 * ```ts
 * await prefetchAppLimitCreditRedemptionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppLimitCreditRedemptionQuery<
  S extends AppLimitCreditRedemptionSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLimitCreditRedemptionSelect>;
  }
): Promise<void>;
export async function prefetchAppLimitCreditRedemptionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppLimitCreditRedemptionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppLimitCreditRedemptionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appLimitCreditRedemptionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLimitCreditRedemption.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
