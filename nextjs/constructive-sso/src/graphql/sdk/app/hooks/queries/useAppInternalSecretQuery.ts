/**
 * Single item query hook for AppInternalSecret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appInternalSecretKeys } from '../query-keys';
import type {
  AppInternalSecretSelect,
  AppInternalSecretWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppInternalSecretSelect,
  AppInternalSecretWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appInternalSecretQueryKey = appInternalSecretKeys.detail;
/**
 * Query hook for fetching a single AppInternalSecret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppInternalSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppInternalSecretQuery<
  S extends AppInternalSecretSelect,
  TData = {
    appInternalSecret: InferSelectResult<AppInternalSecretWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppInternalSecretSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appInternalSecret: InferSelectResult<AppInternalSecretWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppInternalSecretQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppInternalSecretSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppInternalSecretSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appInternalSecretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appInternalSecret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single AppInternalSecret without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchAppInternalSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppInternalSecretQuery<S extends AppInternalSecretSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppInternalSecretSelect>;
}): Promise<{
  appInternalSecret: InferSelectResult<AppInternalSecretWithRelations, S> | null;
}>;
export async function fetchAppInternalSecretQuery(params: {
  id: string;
  selection: SelectionConfig<AppInternalSecretSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppInternalSecretSelect>(params.selection);
  return getClient()
    .appInternalSecret.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single AppInternalSecret for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchAppInternalSecretQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppInternalSecretQuery<S extends AppInternalSecretSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppInternalSecretSelect>;
  }
): Promise<void>;
export async function prefetchAppInternalSecretQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppInternalSecretSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppInternalSecretSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appInternalSecretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appInternalSecret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
