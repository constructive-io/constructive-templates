/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appMembershipProfileKeys } from '../query-keys';
import type {
  AppMembershipProfileSelect,
  AppMembershipProfileWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppMembershipProfileSelect,
  AppMembershipProfileWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appMembershipProfileQueryKey = appMembershipProfileKeys.detail;
/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppMembershipProfileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppMembershipProfileQuery<
  S extends AppMembershipProfileSelect,
  TData = {
    appMembershipProfile: InferSelectResult<AppMembershipProfileWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppMembershipProfileSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appMembershipProfile: InferSelectResult<AppMembershipProfileWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppMembershipProfileQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppMembershipProfileSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppMembershipProfileSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appMembershipProfileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appMembershipProfile.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 *
 * @example
 * ```ts
 * const data = await fetchAppMembershipProfileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppMembershipProfileQuery<S extends AppMembershipProfileSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppMembershipProfileSelect>;
}): Promise<{
  appMembershipProfile: InferSelectResult<AppMembershipProfileWithRelations, S> | null;
}>;
export async function fetchAppMembershipProfileQuery(params: {
  id: string;
  selection: SelectionConfig<AppMembershipProfileSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppMembershipProfileSelect>(params.selection);
  return getClient()
    .appMembershipProfile.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Every profile a membership holds; memberships.profile_id points at one of them
 *
 * @example
 * ```ts
 * await prefetchAppMembershipProfileQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppMembershipProfileQuery<S extends AppMembershipProfileSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppMembershipProfileSelect>;
  }
): Promise<void>;
export async function prefetchAppMembershipProfileQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppMembershipProfileSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppMembershipProfileSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appMembershipProfileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appMembershipProfile.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
