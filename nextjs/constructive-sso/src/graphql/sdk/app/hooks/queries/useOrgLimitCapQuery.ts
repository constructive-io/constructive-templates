/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgLimitCapKeys } from '../query-keys';
import type { OrgLimitCapSelect, OrgLimitCapWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgLimitCapSelect, OrgLimitCapWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgLimitCapQueryKey = orgLimitCapKeys.detail;
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgLimitCapQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgLimitCapQuery<
  S extends OrgLimitCapSelect,
  TData = {
    orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCapSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgLimitCapQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitCapSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgLimitCapSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgLimitCapKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimitCap.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```ts
 * const data = await fetchOrgLimitCapQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgLimitCapQuery<S extends OrgLimitCapSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgLimitCapSelect>;
}): Promise<{
  orgLimitCap: InferSelectResult<OrgLimitCapWithRelations, S> | null;
}>;
export async function fetchOrgLimitCapQuery(params: {
  id: string;
  selection: SelectionConfig<OrgLimitCapSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgLimitCapSelect>(params.selection);
  return getClient()
    .orgLimitCap.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Per-entity cap overrides. Allows specific orgs/entities to have different cap values than the scope default.
 *
 * @example
 * ```ts
 * await prefetchOrgLimitCapQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgLimitCapQuery<S extends OrgLimitCapSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgLimitCapSelect>;
  }
): Promise<void>;
export async function prefetchOrgLimitCapQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgLimitCapSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgLimitCapSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgLimitCapKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgLimitCap.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
