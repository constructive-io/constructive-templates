/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileGrantKeys } from '../query-keys';
import type { OrgProfileGrantSelect, OrgProfileGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgProfileGrantSelect, OrgProfileGrantWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgProfileGrantQueryKey = orgProfileGrantKeys.detail;
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgProfileGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgProfileGrantQuery<
  S extends OrgProfileGrantSelect,
  TData = {
    orgProfileGrant: InferSelectResult<OrgProfileGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgProfileGrant: InferSelectResult<OrgProfileGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgProfileGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgProfileGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgProfileGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * const data = await fetchOrgProfileGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgProfileGrantQuery<S extends OrgProfileGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
}): Promise<{
  orgProfileGrant: InferSelectResult<OrgProfileGrantWithRelations, S> | null;
}>;
export async function fetchOrgProfileGrantQuery(params: {
  id: string;
  selection: SelectionConfig<OrgProfileGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgProfileGrantSelect>(params.selection);
  return getClient()
    .orgProfileGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * await prefetchOrgProfileGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgProfileGrantQuery<S extends OrgProfileGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgProfileGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgProfileGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgProfileGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
