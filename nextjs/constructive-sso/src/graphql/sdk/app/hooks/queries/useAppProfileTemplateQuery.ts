/**
 * Template profiles that are automatically seeded into new entities when created
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileTemplateKeys } from '../query-keys';
import type {
  AppProfileTemplateSelect,
  AppProfileTemplateWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfileTemplateSelect,
  AppProfileTemplateWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfileTemplateQueryKey = appProfileTemplateKeys.detail;
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfileTemplateQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppProfileTemplateQuery<
  S extends AppProfileTemplateSelect,
  TData = {
    appProfileTemplate: InferSelectResult<AppProfileTemplateWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileTemplateSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfileTemplate: InferSelectResult<AppProfileTemplateWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfileTemplateQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppProfileTemplateSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppProfileTemplateSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfileTemplateKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileTemplate.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```ts
 * const data = await fetchAppProfileTemplateQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppProfileTemplateQuery<S extends AppProfileTemplateSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppProfileTemplateSelect>;
}): Promise<{
  appProfileTemplate: InferSelectResult<AppProfileTemplateWithRelations, S> | null;
}>;
export async function fetchAppProfileTemplateQuery(params: {
  id: string;
  selection: SelectionConfig<AppProfileTemplateSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppProfileTemplateSelect>(params.selection);
  return getClient()
    .appProfileTemplate.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```ts
 * await prefetchAppProfileTemplateQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppProfileTemplateQuery<S extends AppProfileTemplateSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileTemplateSelect>;
  }
): Promise<void>;
export async function prefetchAppProfileTemplateQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppProfileTemplateSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppProfileTemplateSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfileTemplateKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileTemplate.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
