/**
 * Single item query hook for UserConnectedAccount
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { userConnectedAccountKeys } from '../query-keys';
import type {
  UserConnectedAccountSelect,
  UserConnectedAccountWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  UserConnectedAccountSelect,
  UserConnectedAccountWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const userConnectedAccountQueryKey = userConnectedAccountKeys.detail;
/**
 * Query hook for fetching a single UserConnectedAccount
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserConnectedAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useUserConnectedAccountQuery<
  S extends UserConnectedAccountSelect,
  TData = {
    userConnectedAccount: InferSelectResult<UserConnectedAccountWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UserConnectedAccountSelect>;
  } & Omit<
    UseQueryOptions<
      {
        userConnectedAccount: InferSelectResult<UserConnectedAccountWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useUserConnectedAccountQuery(
  params: {
    id: string;
    selection: SelectionConfig<UserConnectedAccountSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<UserConnectedAccountSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: userConnectedAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .userConnectedAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single UserConnectedAccount without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUserConnectedAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchUserConnectedAccountQuery<S extends UserConnectedAccountSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, UserConnectedAccountSelect>;
}): Promise<{
  userConnectedAccount: InferSelectResult<UserConnectedAccountWithRelations, S> | null;
}>;
export async function fetchUserConnectedAccountQuery(params: {
  id: string;
  selection: SelectionConfig<UserConnectedAccountSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<UserConnectedAccountSelect>(params.selection);
  return getClient()
    .userConnectedAccount.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single UserConnectedAccount for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUserConnectedAccountQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchUserConnectedAccountQuery<S extends UserConnectedAccountSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UserConnectedAccountSelect>;
  }
): Promise<void>;
export async function prefetchUserConnectedAccountQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<UserConnectedAccountSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<UserConnectedAccountSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: userConnectedAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .userConnectedAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
