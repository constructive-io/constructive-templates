/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appBucketKeys } from '../query-keys';
import type { AppBucketSelect, AppBucketWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppBucketSelect, AppBucketWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appBucketQueryKey = appBucketKeys.detail;
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppBucketQuery<
  S extends AppBucketSelect,
  TData = {
    appBucket: InferSelectResult<AppBucketWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appBucket: InferSelectResult<AppBucketWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppBucketQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppBucketSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appBucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appBucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * const data = await fetchAppBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppBucketQuery<S extends AppBucketSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
}): Promise<{
  appBucket: InferSelectResult<AppBucketWithRelations, S> | null;
}>;
export async function fetchAppBucketQuery(params: {
  id: string;
  selection: SelectionConfig<AppBucketSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  return getClient()
    .appBucket.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * await prefetchAppBucketQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppBucketQuery<S extends AppBucketSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppBucketSelect>;
  }
): Promise<void>;
export async function prefetchAppBucketQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppBucketSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppBucketSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appBucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appBucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
