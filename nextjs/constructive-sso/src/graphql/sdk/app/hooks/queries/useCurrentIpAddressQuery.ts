/**
 * Custom query hook for currentIpAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
/** Query key factory - re-exported from query-keys.ts */
export const currentIpAddressQueryKey = customQueryKeys.currentIpAddress;
/**
 * Query hook for currentIpAddress
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCurrentIpAddressQuery();
 *
 * if (data?.currentIpAddress) {
 *   console.log(data.currentIpAddress);
 * }
 * ```
 */
export function useCurrentIpAddressQuery<
  TData = {
    currentIpAddress: string | null;
  },
>(
  params?: Omit<
    UseQueryOptions<
      {
        currentIpAddress: string | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCurrentIpAddressQuery<
  TData = {
    currentIpAddress: string | null;
  },
>(
  params?: Omit<
    UseQueryOptions<
      {
        currentIpAddress: string | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const queryOptions = params ?? {};
  return useQuery({
    queryKey: currentIpAddressQueryKey(),
    queryFn: () => getClient().query.currentIpAddress().unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch currentIpAddress without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCurrentIpAddressQuery();
 * ```
 */
export async function fetchCurrentIpAddressQuery() {
  return getClient().query.currentIpAddress().unwrap();
}
/**
 * Prefetch currentIpAddress for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCurrentIpAddressQuery(queryClient);
 * ```
 */
export async function prefetchCurrentIpAddressQuery(queryClient: QueryClient): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: currentIpAddressQueryKey(),
    queryFn: () => getClient().query.currentIpAddress().unwrap(),
  });
}
