/**
 * Audit log of permission additions and removals from the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgPermissionDefaultGrantKeys } from '../query-keys';
import type {
  OrgPermissionDefaultGrantSelect,
  OrgPermissionDefaultGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgPermissionDefaultGrantSelect,
  OrgPermissionDefaultGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgPermissionDefaultGrantQueryKey = orgPermissionDefaultGrantKeys.detail;
/**
 * Audit log of permission additions and removals from the defaults bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionDefaultGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgPermissionDefaultGrantQuery<
  S extends OrgPermissionDefaultGrantSelect,
  TData = {
    orgPermissionDefaultGrant: InferSelectResult<OrgPermissionDefaultGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgPermissionDefaultGrant: InferSelectResult<
          OrgPermissionDefaultGrantWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgPermissionDefaultGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionDefaultGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgPermissionDefaultGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgPermissionDefaultGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermissionDefaultGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of permission additions and removals from the defaults bitmask
 *
 * @example
 * ```ts
 * const data = await fetchOrgPermissionDefaultGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgPermissionDefaultGrantQuery<
  S extends OrgPermissionDefaultGrantSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultGrantSelect>;
}): Promise<{
  orgPermissionDefaultGrant: InferSelectResult<OrgPermissionDefaultGrantWithRelations, S> | null;
}>;
export async function fetchOrgPermissionDefaultGrantQuery(params: {
  id: string;
  selection: SelectionConfig<OrgPermissionDefaultGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgPermissionDefaultGrantSelect>(params.selection);
  return getClient()
    .orgPermissionDefaultGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of permission additions and removals from the defaults bitmask
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionDefaultGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgPermissionDefaultGrantQuery<
  S extends OrgPermissionDefaultGrantSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgPermissionDefaultGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionDefaultGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgPermissionDefaultGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgPermissionDefaultGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermissionDefaultGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
