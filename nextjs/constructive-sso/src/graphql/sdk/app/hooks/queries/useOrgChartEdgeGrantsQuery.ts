/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { orgChartEdgeGrantKeys } from '../query-keys';
import type {
  OrgChartEdgeGrantSelect,
  OrgChartEdgeGrantWithRelations,
  OrgChartEdgeGrantFilter,
  OrgChartEdgeGrantOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OrgChartEdgeGrantSelect,
  OrgChartEdgeGrantWithRelations,
  OrgChartEdgeGrantFilter,
  OrgChartEdgeGrantOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgChartEdgeGrantsQueryKey = orgChartEdgeGrantKeys.list;
/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgChartEdgeGrantsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOrgChartEdgeGrantsQuery<
  S extends OrgChartEdgeGrantSelect,
  TData = {
    orgChartEdgeGrants: ConnectionResult<InferSelectResult<OrgChartEdgeGrantWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgChartEdgeGrantFilter, OrgChartEdgeGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgChartEdgeGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgChartEdgeGrants: ConnectionResult<InferSelectResult<OrgChartEdgeGrantWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgChartEdgeGrantsQuery(
  params: {
    selection: ListSelectionConfig<
      OrgChartEdgeGrantSelect,
      OrgChartEdgeGrantFilter,
      OrgChartEdgeGrantOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    OrgChartEdgeGrantSelect,
    OrgChartEdgeGrantFilter,
    OrgChartEdgeGrantOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgChartEdgeGrantKeys.list(args),
    queryFn: () => getClient().orgChartEdgeGrant.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 *
 * @example
 * ```ts
 * const data = await fetchOrgChartEdgeGrantsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOrgChartEdgeGrantsQuery<S extends OrgChartEdgeGrantSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, OrgChartEdgeGrantFilter, OrgChartEdgeGrantOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, OrgChartEdgeGrantSelect>;
}): Promise<{
  orgChartEdgeGrants: ConnectionResult<InferSelectResult<OrgChartEdgeGrantWithRelations, S>>;
}>;
export async function fetchOrgChartEdgeGrantsQuery(params: {
  selection: ListSelectionConfig<
    OrgChartEdgeGrantSelect,
    OrgChartEdgeGrantFilter,
    OrgChartEdgeGrantOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    OrgChartEdgeGrantSelect,
    OrgChartEdgeGrantFilter,
    OrgChartEdgeGrantOrderBy
  >(params.selection);
  return getClient().orgChartEdgeGrant.findMany(args).unwrap();
}
/**
 * Append-only log of hierarchy edge grants and revocations; triggers apply changes to the edges table
 *
 * @example
 * ```ts
 * await prefetchOrgChartEdgeGrantsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOrgChartEdgeGrantsQuery<S extends OrgChartEdgeGrantSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgChartEdgeGrantFilter, OrgChartEdgeGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgChartEdgeGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgChartEdgeGrantsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      OrgChartEdgeGrantSelect,
      OrgChartEdgeGrantFilter,
      OrgChartEdgeGrantOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    OrgChartEdgeGrantSelect,
    OrgChartEdgeGrantFilter,
    OrgChartEdgeGrantOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgChartEdgeGrantKeys.list(args),
    queryFn: () => getClient().orgChartEdgeGrant.findMany(args).unwrap(),
  });
}
