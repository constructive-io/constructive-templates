/**
 * GraphQL SDK
 * @generated by @constructive-io/graphql-codegen
 *
 * Tables: OrgGetManagersRecord, OrgGetSubordinatesRecord, AppPermission, AppPermissionDefaultGrant, AppMembership, AppProfilePermission, AppProfileGrant, AppProfileDefinitionGrant, AppInvite, AppAdminGrant, AppOwnerGrant, AppGrant, OrgMembership, OrgProfilePermission, OrgPermissionDefaultPermission, OrgPermissionDefaultGrant, OrgProfileDefinitionGrant, OrgProfileGrant, OrgInvite, OrgMember, OrgAdminGrant, OrgOwnerGrant, OrgMemberProfile, OrgGrant, OrgChartEdge, OrgChartEdgeGrant, OrgPermissionDefault, AppLimit, AppLimitCredit, AppLimitCreditCodeItem, AppLimitCreditRedemption, OrgLimit, OrgLimitCredit, OrgLimitAggregate, OrgLimitWarning, OrgProfile, Principal, PrincipalEntity, PrincipalScopeOverride, Email, PhoneNumber, WebauthnCredential, AppClaimedInvite, OrgClaimedInvite, AuditLogAuth, OrgPermission, IdentityProvider, AppPermissionDefaultPermission, AppPermissionDefault, RoleType, AppMembershipDefault, OrgMembershipDefault, AppLimitCapsDefault, OrgLimitCapsDefault, AppLimitCap, OrgLimitCap, UserConnectedAccount, AppLimitDefault, OrgLimitDefault, AppLimitCreditCode, AppLimitWarning, MembershipType, AppInternalSecret, AppProfileTemplate, OrgProfileTemplate, OrgApiKeyList, AppProfile, AppBucket, AppFile, OrgMembershipSetting, AppLimitEvent, OrgLimitEvent, User
 *
 * Usage:
 *
 * 1. Configure the client:
 * ```ts
 * import { configure } from './generated';
 *
 * configure({
 *   endpoint: 'https://api.example.com/graphql',
 *   headers: { Authorization: 'Bearer <token>' },
 * });
 * ```
 *
 * 2. Use the hooks:
 * ```tsx
 * import { useCarsQuery, useCreateCarMutation } from './generated';
 *
 * function MyComponent() {
 *   const { data, isLoading } = useCarsQuery({
 *     selection: { fields: { id: true }, first: 10 },
 *   });
 *   const { mutate } = useCreateCarMutation({
 *     selection: { fields: { id: true } },
 *   });
 *   // ...
 * }
 * ```
 */
export * from './client';
export * from './query-keys';
export * from './mutation-keys';
export * from './invalidation';
export * from './queries';
export * from './mutations';
