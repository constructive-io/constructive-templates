/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMemberProfileKeys } from '../query-keys';
import { orgMemberProfileMutationKeys } from '../mutation-keys';
import type {
  OrgMemberProfileSelect,
  OrgMemberProfileWithRelations,
  CreateOrgMemberProfileInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMemberProfileSelect,
  OrgMemberProfileWithRelations,
  CreateOrgMemberProfileInput,
} from '../../orm/input-types';
/**
 * Per-membership profile information visible to other entity members (display name, email, title, bio, avatar)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgMemberProfileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgMemberProfileMutation<S extends OrgMemberProfileSelect>(
  params: {
    selection: {
      fields: S & OrgMemberProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgMemberProfile: {
          orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
        };
      },
      Error,
      CreateOrgMemberProfileInput['orgMemberProfile']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgMemberProfile: {
      orgMemberProfile: InferSelectResult<OrgMemberProfileWithRelations, S>;
    };
  },
  Error,
  CreateOrgMemberProfileInput['orgMemberProfile']
>;
export function useCreateOrgMemberProfileMutation(
  params: {
    selection: SelectionConfig<OrgMemberProfileSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOrgMemberProfileInput['orgMemberProfile']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMemberProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMemberProfileMutationKeys.create(),
    mutationFn: (data: CreateOrgMemberProfileInput['orgMemberProfile']) =>
      getClient()
        .orgMemberProfile.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgMemberProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
