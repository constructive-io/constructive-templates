/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileKeys } from '../query-keys';
import { appProfileMutationKeys } from '../mutation-keys';
import type {
  AppProfileSelect,
  AppProfileWithRelations,
  CreateAppProfileInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfileSelect,
  AppProfileWithRelations,
  CreateAppProfileInput,
} from '../../orm/input-types';
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppProfileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppProfileMutation<S extends AppProfileSelect>(
  params: {
    selection: {
      fields: S & AppProfileSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppProfile: {
          appProfile: InferSelectResult<AppProfileWithRelations, S>;
        };
      },
      Error,
      CreateAppProfileInput['appProfile']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppProfile: {
      appProfile: InferSelectResult<AppProfileWithRelations, S>;
    };
  },
  Error,
  CreateAppProfileInput['appProfile']
>;
export function useCreateAppProfileMutation(
  params: {
    selection: SelectionConfig<AppProfileSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppProfileInput['appProfile']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfileMutationKeys.create(),
    mutationFn: (data: CreateAppProfileInput['appProfile']) =>
      getClient()
        .appProfile.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
