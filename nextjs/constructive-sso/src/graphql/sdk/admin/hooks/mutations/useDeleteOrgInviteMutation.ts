/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgInviteKeys } from '../query-keys';
import { orgInviteMutationKeys } from '../mutation-keys';
import type { OrgInviteSelect, OrgInviteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgInviteSelect, OrgInviteWithRelations } from '../../orm/input-types';
/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgInviteMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgInviteMutation<S extends OrgInviteSelect>(
  params: {
    selection: {
      fields: S & OrgInviteSelect;
    } & HookStrictSelect<NoInfer<S>, OrgInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgInvite: {
          orgInvite: InferSelectResult<OrgInviteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgInvite: {
      orgInvite: InferSelectResult<OrgInviteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgInviteMutation(
  params: {
    selection: SelectionConfig<OrgInviteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgInviteMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgInvite.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgInviteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
