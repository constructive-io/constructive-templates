/**
 * Records of individual permission grants and revocations for members via bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appGrantKeys } from '../query-keys';
import { appGrantMutationKeys } from '../mutation-keys';
import type {
  AppGrantSelect,
  AppGrantWithRelations,
  CreateAppGrantInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppGrantSelect,
  AppGrantWithRelations,
  CreateAppGrantInput,
} from '../../orm/input-types';
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppGrantMutation<S extends AppGrantSelect>(
  params: {
    selection: {
      fields: S & AppGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppGrant: {
          appGrant: InferSelectResult<AppGrantWithRelations, S>;
        };
      },
      Error,
      CreateAppGrantInput['appGrant']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppGrant: {
      appGrant: InferSelectResult<AppGrantWithRelations, S>;
    };
  },
  Error,
  CreateAppGrantInput['appGrant']
>;
export function useCreateAppGrantMutation(
  params: {
    selection: SelectionConfig<AppGrantSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppGrantInput['appGrant']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appGrantMutationKeys.create(),
    mutationFn: (data: CreateAppGrantInput['appGrant']) =>
      getClient()
        .appGrant.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
