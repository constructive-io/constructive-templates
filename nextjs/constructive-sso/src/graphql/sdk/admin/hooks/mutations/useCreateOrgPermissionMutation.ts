/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgPermissionKeys } from '../query-keys';
import { orgPermissionMutationKeys } from '../mutation-keys';
import type {
  OrgPermissionSelect,
  OrgPermissionWithRelations,
  CreateOrgPermissionInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgPermissionSelect,
  OrgPermissionWithRelations,
  CreateOrgPermissionInput,
} from '../../orm/input-types';
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgPermissionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgPermissionMutation<S extends OrgPermissionSelect>(
  params: {
    selection: {
      fields: S & OrgPermissionSelect;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgPermission: {
          orgPermission: InferSelectResult<OrgPermissionWithRelations, S>;
        };
      },
      Error,
      CreateOrgPermissionInput['orgPermission']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgPermission: {
      orgPermission: InferSelectResult<OrgPermissionWithRelations, S>;
    };
  },
  Error,
  CreateOrgPermissionInput['orgPermission']
>;
export function useCreateOrgPermissionMutation(
  params: {
    selection: SelectionConfig<OrgPermissionSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgPermissionInput['orgPermission']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgPermissionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgPermissionMutationKeys.create(),
    mutationFn: (data: CreateOrgPermissionInput['orgPermission']) =>
      getClient()
        .orgPermission.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgPermissionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
