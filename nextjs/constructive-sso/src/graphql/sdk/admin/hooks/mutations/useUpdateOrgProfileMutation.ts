/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileKeys } from '../query-keys';
import { orgProfileMutationKeys } from '../mutation-keys';
import type {
  OrgProfileSelect,
  OrgProfileWithRelations,
  OrgProfilePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileSelect,
  OrgProfileWithRelations,
  OrgProfilePatch,
} from '../../orm/input-types';
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgProfileMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgProfilePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgProfileMutation<S extends OrgProfileSelect>(
  params: {
    selection: {
      fields: S & OrgProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgProfile: {
          orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgProfilePatch: OrgProfilePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgProfile: {
      orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgProfilePatch: OrgProfilePatch;
  }
>;
export function useUpdateOrgProfileMutation(
  params: {
    selection: SelectionConfig<OrgProfileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgProfilePatch: OrgProfilePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileMutationKeys.all,
    mutationFn: ({ id, orgProfilePatch }: { id: string; orgProfilePatch: OrgProfilePatch }) =>
      getClient()
        .orgProfile.update({
          where: {
            id,
          },
          data: orgProfilePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgProfileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
