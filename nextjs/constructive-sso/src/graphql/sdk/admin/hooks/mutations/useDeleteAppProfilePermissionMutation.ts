/**
 * Join table linking profiles to individual permissions they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfilePermissionKeys } from '../query-keys';
import { appProfilePermissionMutationKeys } from '../mutation-keys';
import type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
} from '../../orm/input-types';
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppProfilePermissionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppProfilePermissionMutation<S extends AppProfilePermissionSelect>(
  params: {
    selection: {
      fields: S & AppProfilePermissionSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppProfilePermission: {
          appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppProfilePermission: {
      appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppProfilePermissionMutation(
  params: {
    selection: SelectionConfig<AppProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppProfilePermissionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfilePermissionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appProfilePermission.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appProfilePermissionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appProfilePermissionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
