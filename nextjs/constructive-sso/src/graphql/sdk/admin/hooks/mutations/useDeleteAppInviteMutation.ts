/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appInviteKeys } from '../query-keys';
import { appInviteMutationKeys } from '../mutation-keys';
import type { AppInviteSelect, AppInviteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppInviteSelect, AppInviteWithRelations } from '../../orm/input-types';
/**
 * Invitation records sent to prospective members via email, with token-based redemption and expiration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppInviteMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppInviteMutation<S extends AppInviteSelect>(
  params: {
    selection: {
      fields: S & AppInviteSelect;
    } & HookStrictSelect<NoInfer<S>, AppInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppInvite: {
          appInvite: InferSelectResult<AppInviteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppInvite: {
      appInvite: InferSelectResult<AppInviteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppInviteMutation(
  params: {
    selection: SelectionConfig<AppInviteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appInviteMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appInvite.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appInviteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
