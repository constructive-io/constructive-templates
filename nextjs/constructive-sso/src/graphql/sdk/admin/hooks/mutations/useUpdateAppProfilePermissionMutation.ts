/**
 * Join table linking profiles to individual permissions they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfilePermissionKeys } from '../query-keys';
import { appProfilePermissionMutationKeys } from '../mutation-keys';
import type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
  AppProfilePermissionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfilePermissionSelect,
  AppProfilePermissionWithRelations,
  AppProfilePermissionPatch,
} from '../../orm/input-types';
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppProfilePermissionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appProfilePermissionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppProfilePermissionMutation<S extends AppProfilePermissionSelect>(
  params: {
    selection: {
      fields: S & AppProfilePermissionSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppProfilePermission: {
          appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appProfilePermissionPatch: AppProfilePermissionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppProfilePermission: {
      appProfilePermission: InferSelectResult<AppProfilePermissionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appProfilePermissionPatch: AppProfilePermissionPatch;
  }
>;
export function useUpdateAppProfilePermissionMutation(
  params: {
    selection: SelectionConfig<AppProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appProfilePermissionPatch: AppProfilePermissionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppProfilePermissionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfilePermissionMutationKeys.all,
    mutationFn: ({
      id,
      appProfilePermissionPatch,
    }: {
      id: string;
      appProfilePermissionPatch: AppProfilePermissionPatch;
    }) =>
      getClient()
        .appProfilePermission.update({
          where: {
            id,
          },
          data: appProfilePermissionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appProfilePermissionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appProfilePermissionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
