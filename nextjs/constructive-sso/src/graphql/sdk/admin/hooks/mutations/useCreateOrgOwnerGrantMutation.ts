/**
 * Records of ownership transfers and grants between members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgOwnerGrantKeys } from '../query-keys';
import { orgOwnerGrantMutationKeys } from '../mutation-keys';
import type {
  OrgOwnerGrantSelect,
  OrgOwnerGrantWithRelations,
  CreateOrgOwnerGrantInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgOwnerGrantSelect,
  OrgOwnerGrantWithRelations,
  CreateOrgOwnerGrantInput,
} from '../../orm/input-types';
/**
 * Records of ownership transfers and grants between members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgOwnerGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgOwnerGrantMutation<S extends OrgOwnerGrantSelect>(
  params: {
    selection: {
      fields: S & OrgOwnerGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgOwnerGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgOwnerGrant: {
          orgOwnerGrant: InferSelectResult<OrgOwnerGrantWithRelations, S>;
        };
      },
      Error,
      CreateOrgOwnerGrantInput['orgOwnerGrant']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgOwnerGrant: {
      orgOwnerGrant: InferSelectResult<OrgOwnerGrantWithRelations, S>;
    };
  },
  Error,
  CreateOrgOwnerGrantInput['orgOwnerGrant']
>;
export function useCreateOrgOwnerGrantMutation(
  params: {
    selection: SelectionConfig<OrgOwnerGrantSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgOwnerGrantInput['orgOwnerGrant']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgOwnerGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgOwnerGrantMutationKeys.create(),
    mutationFn: (data: CreateOrgOwnerGrantInput['orgOwnerGrant']) =>
      getClient()
        .orgOwnerGrant.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgOwnerGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
