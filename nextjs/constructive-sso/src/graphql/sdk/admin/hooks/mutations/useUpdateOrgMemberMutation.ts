/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgMemberKeys } from '../query-keys';
import { orgMemberMutationKeys } from '../mutation-keys';
import type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  OrgMemberPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  OrgMemberPatch,
} from '../../orm/input-types';
/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgMemberMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgMemberPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgMemberMutation<S extends OrgMemberSelect>(
  params: {
    selection: {
      fields: S & OrgMemberSelect;
    } & HookStrictSelect<NoInfer<S>, OrgMemberSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgMember: {
          orgMember: InferSelectResult<OrgMemberWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgMemberPatch: OrgMemberPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgMember: {
      orgMember: InferSelectResult<OrgMemberWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgMemberPatch: OrgMemberPatch;
  }
>;
export function useUpdateOrgMemberMutation(
  params: {
    selection: SelectionConfig<OrgMemberSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgMemberPatch: OrgMemberPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgMemberSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgMemberMutationKeys.all,
    mutationFn: ({ id, orgMemberPatch }: { id: string; orgMemberPatch: OrgMemberPatch }) =>
      getClient()
        .orgMember.update({
          where: {
            id,
          },
          data: orgMemberPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgMemberKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgMemberKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
