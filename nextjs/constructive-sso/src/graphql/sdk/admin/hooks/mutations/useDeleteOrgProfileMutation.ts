/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileKeys } from '../query-keys';
import { orgProfileMutationKeys } from '../mutation-keys';
import type { OrgProfileSelect, OrgProfileWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgProfileSelect, OrgProfileWithRelations } from '../../orm/input-types';
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgProfileMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgProfileMutation<S extends OrgProfileSelect>(
  params: {
    selection: {
      fields: S & OrgProfileSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgProfile: {
          orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgProfile: {
      orgProfile: InferSelectResult<OrgProfileWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgProfileMutation(
  params: {
    selection: SelectionConfig<OrgProfileSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgProfile.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgProfileKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfileKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
