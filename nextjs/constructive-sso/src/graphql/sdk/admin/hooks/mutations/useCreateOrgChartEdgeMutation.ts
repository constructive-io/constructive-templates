/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgChartEdgeKeys } from '../query-keys';
import { orgChartEdgeMutationKeys } from '../mutation-keys';
import type {
  OrgChartEdgeSelect,
  OrgChartEdgeWithRelations,
  CreateOrgChartEdgeInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgChartEdgeSelect,
  OrgChartEdgeWithRelations,
  CreateOrgChartEdgeInput,
} from '../../orm/input-types';
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgChartEdgeMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgChartEdgeMutation<S extends OrgChartEdgeSelect>(
  params: {
    selection: {
      fields: S & OrgChartEdgeSelect;
    } & HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgChartEdge: {
          orgChartEdge: InferSelectResult<OrgChartEdgeWithRelations, S>;
        };
      },
      Error,
      CreateOrgChartEdgeInput['orgChartEdge']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgChartEdge: {
      orgChartEdge: InferSelectResult<OrgChartEdgeWithRelations, S>;
    };
  },
  Error,
  CreateOrgChartEdgeInput['orgChartEdge']
>;
export function useCreateOrgChartEdgeMutation(
  params: {
    selection: SelectionConfig<OrgChartEdgeSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateOrgChartEdgeInput['orgChartEdge']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<OrgChartEdgeSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgChartEdgeMutationKeys.create(),
    mutationFn: (data: CreateOrgChartEdgeInput['orgChartEdge']) =>
      getClient()
        .orgChartEdge.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgChartEdgeKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
