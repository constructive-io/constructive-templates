/**
 * Join table linking profiles to individual permissions they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfilePermissionKeys } from '../query-keys';
import { orgProfilePermissionMutationKeys } from '../mutation-keys';
import type {
  OrgProfilePermissionSelect,
  OrgProfilePermissionWithRelations,
  OrgProfilePermissionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfilePermissionSelect,
  OrgProfilePermissionWithRelations,
  OrgProfilePermissionPatch,
} from '../../orm/input-types';
/**
 * Join table linking profiles to individual permissions they include
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgProfilePermissionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgProfilePermissionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgProfilePermissionMutation<S extends OrgProfilePermissionSelect>(
  params: {
    selection: {
      fields: S & OrgProfilePermissionSelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgProfilePermission: {
          orgProfilePermission: InferSelectResult<OrgProfilePermissionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgProfilePermissionPatch: OrgProfilePermissionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgProfilePermission: {
      orgProfilePermission: InferSelectResult<OrgProfilePermissionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgProfilePermissionPatch: OrgProfilePermissionPatch;
  }
>;
export function useUpdateOrgProfilePermissionMutation(
  params: {
    selection: SelectionConfig<OrgProfilePermissionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgProfilePermissionPatch: OrgProfilePermissionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfilePermissionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfilePermissionMutationKeys.all,
    mutationFn: ({
      id,
      orgProfilePermissionPatch,
    }: {
      id: string;
      orgProfilePermissionPatch: OrgProfilePermissionPatch;
    }) =>
      getClient()
        .orgProfilePermission.update({
          where: {
            id,
          },
          data: orgProfilePermissionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgProfilePermissionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfilePermissionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
