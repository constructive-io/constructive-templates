/**
 * Records of successfully claimed invitations, linking senders to receivers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgClaimedInviteKeys } from '../query-keys';
import { orgClaimedInviteMutationKeys } from '../mutation-keys';
import type {
  OrgClaimedInviteSelect,
  OrgClaimedInviteWithRelations,
  CreateOrgClaimedInviteInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgClaimedInviteSelect,
  OrgClaimedInviteWithRelations,
  CreateOrgClaimedInviteInput,
} from '../../orm/input-types';
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgClaimedInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgClaimedInviteMutation<S extends OrgClaimedInviteSelect>(
  params: {
    selection: {
      fields: S & OrgClaimedInviteSelect;
    } & HookStrictSelect<NoInfer<S>, OrgClaimedInviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgClaimedInvite: {
          orgClaimedInvite: InferSelectResult<OrgClaimedInviteWithRelations, S>;
        };
      },
      Error,
      CreateOrgClaimedInviteInput['orgClaimedInvite']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgClaimedInvite: {
      orgClaimedInvite: InferSelectResult<OrgClaimedInviteWithRelations, S>;
    };
  },
  Error,
  CreateOrgClaimedInviteInput['orgClaimedInvite']
>;
export function useCreateOrgClaimedInviteMutation(
  params: {
    selection: SelectionConfig<OrgClaimedInviteSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOrgClaimedInviteInput['orgClaimedInvite']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgClaimedInviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgClaimedInviteMutationKeys.create(),
    mutationFn: (data: CreateOrgClaimedInviteInput['orgClaimedInvite']) =>
      getClient()
        .orgClaimedInvite.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgClaimedInviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
