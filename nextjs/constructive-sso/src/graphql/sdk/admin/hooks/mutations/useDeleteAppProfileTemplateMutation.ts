/**
 * Template profiles that are automatically seeded into new entities when created
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileTemplateKeys } from '../query-keys';
import { appProfileTemplateMutationKeys } from '../mutation-keys';
import type {
  AppProfileTemplateSelect,
  AppProfileTemplateWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfileTemplateSelect,
  AppProfileTemplateWithRelations,
} from '../../orm/input-types';
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppProfileTemplateMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppProfileTemplateMutation<S extends AppProfileTemplateSelect>(
  params: {
    selection: {
      fields: S & AppProfileTemplateSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfileTemplateSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppProfileTemplate: {
          appProfileTemplate: InferSelectResult<AppProfileTemplateWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppProfileTemplate: {
      appProfileTemplate: InferSelectResult<AppProfileTemplateWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppProfileTemplateMutation(
  params: {
    selection: SelectionConfig<AppProfileTemplateSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppProfileTemplateSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfileTemplateMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appProfileTemplate.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appProfileTemplateKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appProfileTemplateKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
