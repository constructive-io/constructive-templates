/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileGrantKeys } from '../query-keys';
import { appProfileGrantMutationKeys } from '../mutation-keys';
import type { AppProfileGrantSelect, AppProfileGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppProfileGrantSelect, AppProfileGrantWithRelations } from '../../orm/input-types';
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppProfileGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppProfileGrantMutation<S extends AppProfileGrantSelect>(
  params: {
    selection: {
      fields: S & AppProfileGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppProfileGrant: {
          appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppProfileGrant: {
      appProfileGrant: InferSelectResult<AppProfileGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppProfileGrantMutation(
  params: {
    selection: SelectionConfig<AppProfileGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppProfileGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appProfileGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appProfileGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appProfileGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appProfileGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
