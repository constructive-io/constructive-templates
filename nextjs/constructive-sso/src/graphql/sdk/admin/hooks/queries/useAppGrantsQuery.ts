/**
 * Records of individual permission grants and revocations for members via bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appGrantKeys } from '../query-keys';
import type {
  AppGrantSelect,
  AppGrantWithRelations,
  AppGrantFilter,
  AppGrantOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppGrantSelect,
  AppGrantWithRelations,
  AppGrantFilter,
  AppGrantOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appGrantsQueryKey = appGrantKeys.list;
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppGrantsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppGrantsQuery<
  S extends AppGrantSelect,
  TData = {
    appGrants: ConnectionResult<InferSelectResult<AppGrantWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppGrantFilter, AppGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appGrants: ConnectionResult<InferSelectResult<AppGrantWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppGrantsQuery(
  params: {
    selection: ListSelectionConfig<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appGrantKeys.list(args),
    queryFn: () => getClient().appGrant.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```ts
 * const data = await fetchAppGrantsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppGrantsQuery<S extends AppGrantSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppGrantFilter, AppGrantOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppGrantSelect>;
}): Promise<{
  appGrants: ConnectionResult<InferSelectResult<AppGrantWithRelations, S>>;
}>;
export async function fetchAppGrantsQuery(params: {
  selection: ListSelectionConfig<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>;
}) {
  const args = buildListSelectionArgs<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>(
    params.selection
  );
  return getClient().appGrant.findMany(args).unwrap();
}
/**
 * Records of individual permission grants and revocations for members via bitmask
 *
 * @example
 * ```ts
 * await prefetchAppGrantsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppGrantsQuery<S extends AppGrantSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppGrantFilter, AppGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppGrantSelect>;
  }
): Promise<void>;
export async function prefetchAppGrantsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AppGrantSelect, AppGrantFilter, AppGrantOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: appGrantKeys.list(args),
    queryFn: () => getClient().appGrant.findMany(args).unwrap(),
  });
}
