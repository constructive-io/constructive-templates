/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { orgMemberKeys } from '../query-keys';
import type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  OrgMemberFilter,
  OrgMemberOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OrgMemberSelect,
  OrgMemberWithRelations,
  OrgMemberFilter,
  OrgMemberOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgMembersQueryKey = orgMemberKeys.list;
/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgMembersQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOrgMembersQuery<
  S extends OrgMemberSelect,
  TData = {
    orgMembers: ConnectionResult<InferSelectResult<OrgMemberWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgMemberFilter, OrgMemberOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgMemberSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgMembers: ConnectionResult<InferSelectResult<OrgMemberWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgMembersQuery(
  params: {
    selection: ListSelectionConfig<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgMemberKeys.list(args),
    queryFn: () => getClient().orgMember.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 *
 * @example
 * ```ts
 * const data = await fetchOrgMembersQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOrgMembersQuery<S extends OrgMemberSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, OrgMemberFilter, OrgMemberOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, OrgMemberSelect>;
}): Promise<{
  orgMembers: ConnectionResult<InferSelectResult<OrgMemberWithRelations, S>>;
}>;
export async function fetchOrgMembersQuery(params: {
  selection: ListSelectionConfig<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>;
}) {
  const args = buildListSelectionArgs<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>(
    params.selection
  );
  return getClient().orgMember.findMany(args).unwrap();
}
/**
 * Simplified view of active members in an entity, used for listing who belongs to an org or group
 *
 * @example
 * ```ts
 * await prefetchOrgMembersQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOrgMembersQuery<S extends OrgMemberSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgMemberFilter, OrgMemberOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgMemberSelect>;
  }
): Promise<void>;
export async function prefetchOrgMembersQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<OrgMemberSelect, OrgMemberFilter, OrgMemberOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: orgMemberKeys.list(args),
    queryFn: () => getClient().orgMember.findMany(args).unwrap(),
  });
}
