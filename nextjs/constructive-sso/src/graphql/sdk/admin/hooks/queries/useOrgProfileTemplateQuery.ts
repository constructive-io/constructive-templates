/**
 * Template profiles that are automatically seeded into new entities when created
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileTemplateKeys } from '../query-keys';
import type {
  OrgProfileTemplateSelect,
  OrgProfileTemplateWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileTemplateSelect,
  OrgProfileTemplateWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgProfileTemplateQueryKey = orgProfileTemplateKeys.detail;
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgProfileTemplateQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgProfileTemplateQuery<
  S extends OrgProfileTemplateSelect,
  TData = {
    orgProfileTemplate: InferSelectResult<OrgProfileTemplateWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileTemplateSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgProfileTemplate: InferSelectResult<OrgProfileTemplateWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgProfileTemplateQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileTemplateSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgProfileTemplateSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgProfileTemplateKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileTemplate.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```ts
 * const data = await fetchOrgProfileTemplateQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgProfileTemplateQuery<S extends OrgProfileTemplateSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgProfileTemplateSelect>;
}): Promise<{
  orgProfileTemplate: InferSelectResult<OrgProfileTemplateWithRelations, S> | null;
}>;
export async function fetchOrgProfileTemplateQuery(params: {
  id: string;
  selection: SelectionConfig<OrgProfileTemplateSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgProfileTemplateSelect>(params.selection);
  return getClient()
    .orgProfileTemplate.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Template profiles that are automatically seeded into new entities when created
 *
 * @example
 * ```ts
 * await prefetchOrgProfileTemplateQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgProfileTemplateQuery<S extends OrgProfileTemplateSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgProfileTemplateSelect>;
  }
): Promise<void>;
export async function prefetchOrgProfileTemplateQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgProfileTemplateSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgProfileTemplateSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgProfileTemplateKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgProfileTemplate.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
