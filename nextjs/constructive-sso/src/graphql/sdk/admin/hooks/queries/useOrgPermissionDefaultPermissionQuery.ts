/**
 * Join table linking permission defaults to individual permissions; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgPermissionDefaultPermissionKeys } from '../query-keys';
import type {
  OrgPermissionDefaultPermissionSelect,
  OrgPermissionDefaultPermissionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgPermissionDefaultPermissionSelect,
  OrgPermissionDefaultPermissionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgPermissionDefaultPermissionQueryKey = orgPermissionDefaultPermissionKeys.detail;
/**
 * Join table linking permission defaults to individual permissions; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionDefaultPermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgPermissionDefaultPermissionQuery<
  S extends OrgPermissionDefaultPermissionSelect,
  TData = {
    orgPermissionDefaultPermission: InferSelectResult<
      OrgPermissionDefaultPermissionWithRelations,
      S
    > | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultPermissionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgPermissionDefaultPermission: InferSelectResult<
          OrgPermissionDefaultPermissionWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgPermissionDefaultPermissionQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionDefaultPermissionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgPermissionDefaultPermissionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgPermissionDefaultPermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermissionDefaultPermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table linking permission defaults to individual permissions; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * const data = await fetchOrgPermissionDefaultPermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgPermissionDefaultPermissionQuery<
  S extends OrgPermissionDefaultPermissionSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultPermissionSelect>;
}): Promise<{
  orgPermissionDefaultPermission: InferSelectResult<
    OrgPermissionDefaultPermissionWithRelations,
    S
  > | null;
}>;
export async function fetchOrgPermissionDefaultPermissionQuery(params: {
  id: string;
  selection: SelectionConfig<OrgPermissionDefaultPermissionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgPermissionDefaultPermissionSelect>(params.selection);
  return getClient()
    .orgPermissionDefaultPermission.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table linking permission defaults to individual permissions; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionDefaultPermissionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgPermissionDefaultPermissionQuery<
  S extends OrgPermissionDefaultPermissionSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionDefaultPermissionSelect>;
  }
): Promise<void>;
export async function prefetchOrgPermissionDefaultPermissionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionDefaultPermissionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgPermissionDefaultPermissionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgPermissionDefaultPermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermissionDefaultPermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
