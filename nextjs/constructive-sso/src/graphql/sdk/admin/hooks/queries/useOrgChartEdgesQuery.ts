/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { orgChartEdgeKeys } from '../query-keys';
import type {
  OrgChartEdgeSelect,
  OrgChartEdgeWithRelations,
  OrgChartEdgeFilter,
  OrgChartEdgeOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OrgChartEdgeSelect,
  OrgChartEdgeWithRelations,
  OrgChartEdgeFilter,
  OrgChartEdgeOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgChartEdgesQueryKey = orgChartEdgeKeys.list;
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgChartEdgesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOrgChartEdgesQuery<
  S extends OrgChartEdgeSelect,
  TData = {
    orgChartEdges: ConnectionResult<InferSelectResult<OrgChartEdgeWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgChartEdgeFilter, OrgChartEdgeOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgChartEdges: ConnectionResult<InferSelectResult<OrgChartEdgeWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgChartEdgesQuery(
  params: {
    selection: ListSelectionConfig<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgChartEdgeKeys.list(args),
    queryFn: () => getClient().orgChartEdge.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```ts
 * const data = await fetchOrgChartEdgesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOrgChartEdgesQuery<S extends OrgChartEdgeSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, OrgChartEdgeFilter, OrgChartEdgeOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
}): Promise<{
  orgChartEdges: ConnectionResult<InferSelectResult<OrgChartEdgeWithRelations, S>>;
}>;
export async function fetchOrgChartEdgesQuery(params: {
  selection: ListSelectionConfig<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>;
}) {
  const args = buildListSelectionArgs<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>(
    params.selection
  );
  return getClient().orgChartEdge.findMany(args).unwrap();
}
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```ts
 * await prefetchOrgChartEdgesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOrgChartEdgesQuery<S extends OrgChartEdgeSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgChartEdgeFilter, OrgChartEdgeOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
  }
): Promise<void>;
export async function prefetchOrgChartEdgesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<OrgChartEdgeSelect, OrgChartEdgeFilter, OrgChartEdgeOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: orgChartEdgeKeys.list(args),
    queryFn: () => getClient().orgChartEdge.findMany(args).unwrap(),
  });
}
