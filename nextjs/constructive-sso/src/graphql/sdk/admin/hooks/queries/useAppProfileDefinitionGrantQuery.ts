/**
 * Audit log of permission additions and removals from profile definitions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileDefinitionGrantKeys } from '../query-keys';
import type {
  AppProfileDefinitionGrantSelect,
  AppProfileDefinitionGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppProfileDefinitionGrantSelect,
  AppProfileDefinitionGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfileDefinitionGrantQueryKey = appProfileDefinitionGrantKeys.detail;
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfileDefinitionGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppProfileDefinitionGrantQuery<
  S extends AppProfileDefinitionGrantSelect,
  TData = {
    appProfileDefinitionGrant: InferSelectResult<AppProfileDefinitionGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileDefinitionGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfileDefinitionGrant: InferSelectResult<
          AppProfileDefinitionGrantWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfileDefinitionGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppProfileDefinitionGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppProfileDefinitionGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfileDefinitionGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileDefinitionGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```ts
 * const data = await fetchAppProfileDefinitionGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppProfileDefinitionGrantQuery<
  S extends AppProfileDefinitionGrantSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppProfileDefinitionGrantSelect>;
}): Promise<{
  appProfileDefinitionGrant: InferSelectResult<AppProfileDefinitionGrantWithRelations, S> | null;
}>;
export async function fetchAppProfileDefinitionGrantQuery(params: {
  id: string;
  selection: SelectionConfig<AppProfileDefinitionGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppProfileDefinitionGrantSelect>(params.selection);
  return getClient()
    .appProfileDefinitionGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of permission additions and removals from profile definitions
 *
 * @example
 * ```ts
 * await prefetchAppProfileDefinitionGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppProfileDefinitionGrantQuery<
  S extends AppProfileDefinitionGrantSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileDefinitionGrantSelect>;
  }
): Promise<void>;
export async function prefetchAppProfileDefinitionGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppProfileDefinitionGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppProfileDefinitionGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfileDefinitionGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfileDefinitionGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
