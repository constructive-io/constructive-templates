/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appProfileGrantKeys } from '../query-keys';
import type {
  AppProfileGrantSelect,
  AppProfileGrantWithRelations,
  AppProfileGrantFilter,
  AppProfileGrantOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppProfileGrantSelect,
  AppProfileGrantWithRelations,
  AppProfileGrantFilter,
  AppProfileGrantOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfileGrantsQueryKey = appProfileGrantKeys.list;
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfileGrantsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppProfileGrantsQuery<
  S extends AppProfileGrantSelect,
  TData = {
    appProfileGrants: ConnectionResult<InferSelectResult<AppProfileGrantWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppProfileGrantFilter, AppProfileGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfileGrants: ConnectionResult<InferSelectResult<AppProfileGrantWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfileGrantsQuery(
  params: {
    selection: ListSelectionConfig<
      AppProfileGrantSelect,
      AppProfileGrantFilter,
      AppProfileGrantOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    AppProfileGrantSelect,
    AppProfileGrantFilter,
    AppProfileGrantOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfileGrantKeys.list(args),
    queryFn: () => getClient().appProfileGrant.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * const data = await fetchAppProfileGrantsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppProfileGrantsQuery<S extends AppProfileGrantSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppProfileGrantFilter, AppProfileGrantOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
}): Promise<{
  appProfileGrants: ConnectionResult<InferSelectResult<AppProfileGrantWithRelations, S>>;
}>;
export async function fetchAppProfileGrantsQuery(params: {
  selection: ListSelectionConfig<
    AppProfileGrantSelect,
    AppProfileGrantFilter,
    AppProfileGrantOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    AppProfileGrantSelect,
    AppProfileGrantFilter,
    AppProfileGrantOrderBy
  >(params.selection);
  return getClient().appProfileGrant.findMany(args).unwrap();
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * await prefetchAppProfileGrantsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppProfileGrantsQuery<S extends AppProfileGrantSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppProfileGrantFilter, AppProfileGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppProfileGrantSelect>;
  }
): Promise<void>;
export async function prefetchAppProfileGrantsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      AppProfileGrantSelect,
      AppProfileGrantFilter,
      AppProfileGrantOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    AppProfileGrantSelect,
    AppProfileGrantFilter,
    AppProfileGrantOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfileGrantKeys.list(args),
    queryFn: () => getClient().appProfileGrant.findMany(args).unwrap(),
  });
}
