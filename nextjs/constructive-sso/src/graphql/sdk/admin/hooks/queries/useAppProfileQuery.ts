/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appProfileKeys } from '../query-keys';
import type { AppProfileSelect, AppProfileWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppProfileSelect, AppProfileWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appProfileQueryKey = appProfileKeys.detail;
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppProfileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppProfileQuery<
  S extends AppProfileSelect,
  TData = {
    appProfile: InferSelectResult<AppProfileWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appProfile: InferSelectResult<AppProfileWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppProfileQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppProfileSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppProfileSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appProfileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfile.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```ts
 * const data = await fetchAppProfileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppProfileQuery<S extends AppProfileSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppProfileSelect>;
}): Promise<{
  appProfile: InferSelectResult<AppProfileWithRelations, S> | null;
}>;
export async function fetchAppProfileQuery(params: {
  id: string;
  selection: SelectionConfig<AppProfileSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppProfileSelect>(params.selection);
  return getClient()
    .appProfile.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named permission bundles (roles) that group multiple permissions into reusable profiles
 *
 * @example
 * ```ts
 * await prefetchAppProfileQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppProfileQuery<S extends AppProfileSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppProfileSelect>;
  }
): Promise<void>;
export async function prefetchAppProfileQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppProfileSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppProfileSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appProfileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appProfile.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
