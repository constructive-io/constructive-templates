/**
 * Records of successfully claimed invitations, linking senders to receivers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appClaimedInviteKeys } from '../query-keys';
import type { AppClaimedInviteSelect, AppClaimedInviteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppClaimedInviteSelect, AppClaimedInviteWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appClaimedInviteQueryKey = appClaimedInviteKeys.detail;
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppClaimedInviteQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppClaimedInviteQuery<
  S extends AppClaimedInviteSelect,
  TData = {
    appClaimedInvite: InferSelectResult<AppClaimedInviteWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppClaimedInviteSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appClaimedInvite: InferSelectResult<AppClaimedInviteWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppClaimedInviteQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppClaimedInviteSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppClaimedInviteSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appClaimedInviteKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appClaimedInvite.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```ts
 * const data = await fetchAppClaimedInviteQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppClaimedInviteQuery<S extends AppClaimedInviteSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppClaimedInviteSelect>;
}): Promise<{
  appClaimedInvite: InferSelectResult<AppClaimedInviteWithRelations, S> | null;
}>;
export async function fetchAppClaimedInviteQuery(params: {
  id: string;
  selection: SelectionConfig<AppClaimedInviteSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppClaimedInviteSelect>(params.selection);
  return getClient()
    .appClaimedInvite.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```ts
 * await prefetchAppClaimedInviteQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppClaimedInviteQuery<S extends AppClaimedInviteSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppClaimedInviteSelect>;
  }
): Promise<void>;
export async function prefetchAppClaimedInviteQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppClaimedInviteSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppClaimedInviteSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appClaimedInviteKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appClaimedInvite.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
