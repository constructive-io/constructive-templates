/**
 * Audit log of profile assignments and revocations for members
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { orgProfileGrantKeys } from '../query-keys';
import type {
  OrgProfileGrantSelect,
  OrgProfileGrantWithRelations,
  OrgProfileGrantFilter,
  OrgProfileGrantOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OrgProfileGrantSelect,
  OrgProfileGrantWithRelations,
  OrgProfileGrantFilter,
  OrgProfileGrantOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgProfileGrantsQueryKey = orgProfileGrantKeys.list;
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgProfileGrantsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOrgProfileGrantsQuery<
  S extends OrgProfileGrantSelect,
  TData = {
    orgProfileGrants: ConnectionResult<InferSelectResult<OrgProfileGrantWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgProfileGrantFilter, OrgProfileGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgProfileGrants: ConnectionResult<InferSelectResult<OrgProfileGrantWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgProfileGrantsQuery(
  params: {
    selection: ListSelectionConfig<
      OrgProfileGrantSelect,
      OrgProfileGrantFilter,
      OrgProfileGrantOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    OrgProfileGrantSelect,
    OrgProfileGrantFilter,
    OrgProfileGrantOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgProfileGrantKeys.list(args),
    queryFn: () => getClient().orgProfileGrant.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * const data = await fetchOrgProfileGrantsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOrgProfileGrantsQuery<S extends OrgProfileGrantSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, OrgProfileGrantFilter, OrgProfileGrantOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
}): Promise<{
  orgProfileGrants: ConnectionResult<InferSelectResult<OrgProfileGrantWithRelations, S>>;
}>;
export async function fetchOrgProfileGrantsQuery(params: {
  selection: ListSelectionConfig<
    OrgProfileGrantSelect,
    OrgProfileGrantFilter,
    OrgProfileGrantOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    OrgProfileGrantSelect,
    OrgProfileGrantFilter,
    OrgProfileGrantOrderBy
  >(params.selection);
  return getClient().orgProfileGrant.findMany(args).unwrap();
}
/**
 * Audit log of profile assignments and revocations for members
 *
 * @example
 * ```ts
 * await prefetchOrgProfileGrantsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOrgProfileGrantsQuery<S extends OrgProfileGrantSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, OrgProfileGrantFilter, OrgProfileGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, OrgProfileGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgProfileGrantsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      OrgProfileGrantSelect,
      OrgProfileGrantFilter,
      OrgProfileGrantOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    OrgProfileGrantSelect,
    OrgProfileGrantFilter,
    OrgProfileGrantOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgProfileGrantKeys.list(args),
    queryFn: () => getClient().orgProfileGrant.findMany(args).unwrap(),
  });
}
