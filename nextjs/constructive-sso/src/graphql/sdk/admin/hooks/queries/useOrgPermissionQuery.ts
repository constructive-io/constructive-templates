/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgPermissionKeys } from '../query-keys';
import type { OrgPermissionSelect, OrgPermissionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgPermissionSelect, OrgPermissionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgPermissionQueryKey = orgPermissionKeys.detail;
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgPermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgPermissionQuery<
  S extends OrgPermissionSelect,
  TData = {
    orgPermission: InferSelectResult<OrgPermissionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgPermission: InferSelectResult<OrgPermissionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgPermissionQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgPermissionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgPermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```ts
 * const data = await fetchOrgPermissionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgPermissionQuery<S extends OrgPermissionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgPermissionSelect>;
}): Promise<{
  orgPermission: InferSelectResult<OrgPermissionWithRelations, S> | null;
}>;
export async function fetchOrgPermissionQuery(params: {
  id: string;
  selection: SelectionConfig<OrgPermissionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgPermissionSelect>(params.selection);
  return getClient()
    .orgPermission.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```ts
 * await prefetchOrgPermissionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgPermissionQuery<S extends OrgPermissionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgPermissionSelect>;
  }
): Promise<void>;
export async function prefetchOrgPermissionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgPermissionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgPermissionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgPermissionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgPermission.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
