/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgChartEdgeKeys } from '../query-keys';
import type { OrgChartEdgeSelect, OrgChartEdgeWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgChartEdgeSelect, OrgChartEdgeWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgChartEdgeQueryKey = orgChartEdgeKeys.detail;
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgChartEdgeQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgChartEdgeQuery<
  S extends OrgChartEdgeSelect,
  TData = {
    orgChartEdge: InferSelectResult<OrgChartEdgeWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgChartEdge: InferSelectResult<OrgChartEdgeWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgChartEdgeQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgChartEdgeSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgChartEdgeSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgChartEdgeKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgChartEdge.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```ts
 * const data = await fetchOrgChartEdgeQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgChartEdgeQuery<S extends OrgChartEdgeSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
}): Promise<{
  orgChartEdge: InferSelectResult<OrgChartEdgeWithRelations, S> | null;
}>;
export async function fetchOrgChartEdgeQuery(params: {
  id: string;
  selection: SelectionConfig<OrgChartEdgeSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgChartEdgeSelect>(params.selection);
  return getClient()
    .orgChartEdge.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Organizational chart edges defining parent-child reporting relationships between members within an entity
 *
 * @example
 * ```ts
 * await prefetchOrgChartEdgeQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgChartEdgeQuery<S extends OrgChartEdgeSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgChartEdgeSelect>;
  }
): Promise<void>;
export async function prefetchOrgChartEdgeQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgChartEdgeSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgChartEdgeSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgChartEdgeKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgChartEdge.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
