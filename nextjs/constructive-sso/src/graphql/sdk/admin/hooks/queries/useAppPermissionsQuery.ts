/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appPermissionKeys } from '../query-keys';
import type {
  AppPermissionSelect,
  AppPermissionWithRelations,
  AppPermissionFilter,
  AppPermissionOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppPermissionSelect,
  AppPermissionWithRelations,
  AppPermissionFilter,
  AppPermissionOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appPermissionsQueryKey = appPermissionKeys.list;
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppPermissionsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppPermissionsQuery<
  S extends AppPermissionSelect,
  TData = {
    appPermissions: ConnectionResult<InferSelectResult<AppPermissionWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppPermissionFilter, AppPermissionOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppPermissionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appPermissions: ConnectionResult<InferSelectResult<AppPermissionWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppPermissionsQuery(
  params: {
    selection: ListSelectionConfig<AppPermissionSelect, AppPermissionFilter, AppPermissionOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    AppPermissionSelect,
    AppPermissionFilter,
    AppPermissionOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appPermissionKeys.list(args),
    queryFn: () => getClient().appPermission.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```ts
 * const data = await fetchAppPermissionsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppPermissionsQuery<S extends AppPermissionSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppPermissionFilter, AppPermissionOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppPermissionSelect>;
}): Promise<{
  appPermissions: ConnectionResult<InferSelectResult<AppPermissionWithRelations, S>>;
}>;
export async function fetchAppPermissionsQuery(params: {
  selection: ListSelectionConfig<AppPermissionSelect, AppPermissionFilter, AppPermissionOrderBy>;
}) {
  const args = buildListSelectionArgs<
    AppPermissionSelect,
    AppPermissionFilter,
    AppPermissionOrderBy
  >(params.selection);
  return getClient().appPermission.findMany(args).unwrap();
}
/**
 * Defines available permissions as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```ts
 * await prefetchAppPermissionsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppPermissionsQuery<S extends AppPermissionSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppPermissionFilter, AppPermissionOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppPermissionSelect>;
  }
): Promise<void>;
export async function prefetchAppPermissionsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AppPermissionSelect, AppPermissionFilter, AppPermissionOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    AppPermissionSelect,
    AppPermissionFilter,
    AppPermissionOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appPermissionKeys.list(args),
    queryFn: () => getClient().appPermission.findMany(args).unwrap(),
  });
}
