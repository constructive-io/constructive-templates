/**
 * User email addresses with verification and primary-email management
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailKeys } from '../query-keys';
import { emailMutationKeys } from '../mutation-keys';
import type { EmailSelect, EmailWithRelations, EmailPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { EmailSelect, EmailWithRelations, EmailPatch } from '../../orm/input-types';
/**
 * User email addresses with verification and primary-email management
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', emailPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateEmailMutation<S extends EmailSelect>(
  params: {
    selection: {
      fields: S & EmailSelect;
    } & HookStrictSelect<NoInfer<S>, EmailSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateEmail: {
          email: InferSelectResult<EmailWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        emailPatch: EmailPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateEmail: {
      email: InferSelectResult<EmailWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    emailPatch: EmailPatch;
  }
>;
export function useUpdateEmailMutation(
  params: {
    selection: SelectionConfig<EmailSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        emailPatch: EmailPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailMutationKeys.all,
    mutationFn: ({ id, emailPatch }: { id: string; emailPatch: EmailPatch }) =>
      getClient()
        .email.update({
          where: {
            id,
          },
          data: emailPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: emailKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
