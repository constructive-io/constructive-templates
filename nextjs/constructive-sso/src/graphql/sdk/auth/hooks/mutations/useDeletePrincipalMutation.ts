/**
 * Scoped sub-identities (API keys and agents) with precomputed SPRT
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { principalKeys } from '../query-keys';
import { principalMutationKeys } from '../mutation-keys';
import type { PrincipalSelect, PrincipalWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PrincipalSelect, PrincipalWithRelations } from '../../orm/input-types';
/**
 * Scoped sub-identities (API keys and agents) with precomputed SPRT
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePrincipalMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ principalId: 'value-to-delete' });
 * ```
 */
export function useDeletePrincipalMutation<S extends PrincipalSelect>(
  params: {
    selection: {
      fields: S & PrincipalSelect;
    } & HookStrictSelect<NoInfer<S>, PrincipalSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePrincipal: {
          principal: InferSelectResult<PrincipalWithRelations, S>;
        };
      },
      Error,
      {
        principalId: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePrincipal: {
      principal: InferSelectResult<PrincipalWithRelations, S>;
    };
  },
  Error,
  {
    principalId: string;
  }
>;
export function useDeletePrincipalMutation(
  params: {
    selection: SelectionConfig<PrincipalSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        principalId: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PrincipalSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: principalMutationKeys.all,
    mutationFn: ({ principalId }: { principalId: string }) =>
      getClient()
        .principal.delete({
          where: {
            principalId,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: principalKeys.detail(variables.principalId),
      });
      queryClient.invalidateQueries({
        queryKey: principalKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
