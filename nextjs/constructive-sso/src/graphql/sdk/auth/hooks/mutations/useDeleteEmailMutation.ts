/**
 * User email addresses with verification and primary-email management
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailKeys } from '../query-keys';
import { emailMutationKeys } from '../mutation-keys';
import type { EmailSelect, EmailWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { EmailSelect, EmailWithRelations } from '../../orm/input-types';
/**
 * User email addresses with verification and primary-email management
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteEmailMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteEmailMutation<S extends EmailSelect>(
  params: {
    selection: {
      fields: S & EmailSelect;
    } & HookStrictSelect<NoInfer<S>, EmailSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteEmail: {
          email: InferSelectResult<EmailWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteEmail: {
      email: InferSelectResult<EmailWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteEmailMutation(
  params: {
    selection: SelectionConfig<EmailSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .email.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: emailKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
