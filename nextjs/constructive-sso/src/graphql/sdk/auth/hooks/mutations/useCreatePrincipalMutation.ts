/**
 * Scoped sub-identities (API keys and agents) with precomputed SPRT
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { principalKeys } from '../query-keys';
import { principalMutationKeys } from '../mutation-keys';
import type {
  PrincipalSelect,
  PrincipalWithRelations,
  CreatePrincipalInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PrincipalSelect,
  PrincipalWithRelations,
  CreatePrincipalInput,
} from '../../orm/input-types';
/**
 * Scoped sub-identities (API keys and agents) with precomputed SPRT
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePrincipalMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePrincipalMutation<S extends PrincipalSelect>(
  params: {
    selection: {
      fields: S & PrincipalSelect;
    } & HookStrictSelect<NoInfer<S>, PrincipalSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPrincipal: {
          principal: InferSelectResult<PrincipalWithRelations, S>;
        };
      },
      Error,
      CreatePrincipalInput['principal']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPrincipal: {
      principal: InferSelectResult<PrincipalWithRelations, S>;
    };
  },
  Error,
  CreatePrincipalInput['principal']
>;
export function useCreatePrincipalMutation(
  params: {
    selection: SelectionConfig<PrincipalSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePrincipalInput['principal']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PrincipalSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: principalMutationKeys.create(),
    mutationFn: (data: CreatePrincipalInput['principal']) =>
      getClient()
        .principal.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: principalKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
