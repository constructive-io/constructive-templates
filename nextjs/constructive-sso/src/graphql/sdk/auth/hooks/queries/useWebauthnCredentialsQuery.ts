/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { webauthnCredentialKeys } from '../query-keys';
import type {
  WebauthnCredentialSelect,
  WebauthnCredentialWithRelations,
  WebauthnCredentialFilter,
  WebauthnCredentialOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  WebauthnCredentialSelect,
  WebauthnCredentialWithRelations,
  WebauthnCredentialFilter,
  WebauthnCredentialOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const webauthnCredentialsQueryKey = webauthnCredentialKeys.list;
/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useWebauthnCredentialsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useWebauthnCredentialsQuery<
  S extends WebauthnCredentialSelect,
  TData = {
    webauthnCredentials: ConnectionResult<InferSelectResult<WebauthnCredentialWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, WebauthnCredentialFilter, WebauthnCredentialOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, WebauthnCredentialSelect>;
  } & Omit<
    UseQueryOptions<
      {
        webauthnCredentials: ConnectionResult<
          InferSelectResult<WebauthnCredentialWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useWebauthnCredentialsQuery(
  params: {
    selection: ListSelectionConfig<
      WebauthnCredentialSelect,
      WebauthnCredentialFilter,
      WebauthnCredentialOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    WebauthnCredentialSelect,
    WebauthnCredentialFilter,
    WebauthnCredentialOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: webauthnCredentialKeys.list(args),
    queryFn: () => getClient().webauthnCredential.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 *
 * @example
 * ```ts
 * const data = await fetchWebauthnCredentialsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchWebauthnCredentialsQuery<S extends WebauthnCredentialSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, WebauthnCredentialFilter, WebauthnCredentialOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, WebauthnCredentialSelect>;
}): Promise<{
  webauthnCredentials: ConnectionResult<InferSelectResult<WebauthnCredentialWithRelations, S>>;
}>;
export async function fetchWebauthnCredentialsQuery(params: {
  selection: ListSelectionConfig<
    WebauthnCredentialSelect,
    WebauthnCredentialFilter,
    WebauthnCredentialOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    WebauthnCredentialSelect,
    WebauthnCredentialFilter,
    WebauthnCredentialOrderBy
  >(params.selection);
  return getClient().webauthnCredential.findMany(args).unwrap();
}
/**
 * WebAuthn/passkey credentials owned by users. One row per registered authenticator (security key, device biometric, synced passkey). Schema mirrors SimpleWebAuthn's canonical Passkey object.
 *
 * @example
 * ```ts
 * await prefetchWebauthnCredentialsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchWebauthnCredentialsQuery<S extends WebauthnCredentialSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, WebauthnCredentialFilter, WebauthnCredentialOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, WebauthnCredentialSelect>;
  }
): Promise<void>;
export async function prefetchWebauthnCredentialsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      WebauthnCredentialSelect,
      WebauthnCredentialFilter,
      WebauthnCredentialOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    WebauthnCredentialSelect,
    WebauthnCredentialFilter,
    WebauthnCredentialOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: webauthnCredentialKeys.list(args),
    queryFn: () => getClient().webauthnCredential.findMany(args).unwrap(),
  });
}
