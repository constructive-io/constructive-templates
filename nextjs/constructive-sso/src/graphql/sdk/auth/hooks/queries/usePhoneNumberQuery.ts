/**
 * User phone numbers with country code, verification, and primary-number management
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { phoneNumberKeys } from '../query-keys';
import type { PhoneNumberSelect, PhoneNumberWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PhoneNumberSelect, PhoneNumberWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const phoneNumberQueryKey = phoneNumberKeys.detail;
/**
 * User phone numbers with country code, verification, and primary-number management
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePhoneNumberQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePhoneNumberQuery<
  S extends PhoneNumberSelect,
  TData = {
    phoneNumber: InferSelectResult<PhoneNumberWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PhoneNumberSelect>;
  } & Omit<
    UseQueryOptions<
      {
        phoneNumber: InferSelectResult<PhoneNumberWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePhoneNumberQuery(
  params: {
    id: string;
    selection: SelectionConfig<PhoneNumberSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PhoneNumberSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: phoneNumberKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .phoneNumber.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * User phone numbers with country code, verification, and primary-number management
 *
 * @example
 * ```ts
 * const data = await fetchPhoneNumberQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPhoneNumberQuery<S extends PhoneNumberSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PhoneNumberSelect>;
}): Promise<{
  phoneNumber: InferSelectResult<PhoneNumberWithRelations, S> | null;
}>;
export async function fetchPhoneNumberQuery(params: {
  id: string;
  selection: SelectionConfig<PhoneNumberSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PhoneNumberSelect>(params.selection);
  return getClient()
    .phoneNumber.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * User phone numbers with country code, verification, and primary-number management
 *
 * @example
 * ```ts
 * await prefetchPhoneNumberQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPhoneNumberQuery<S extends PhoneNumberSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PhoneNumberSelect>;
  }
): Promise<void>;
export async function prefetchPhoneNumberQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PhoneNumberSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PhoneNumberSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: phoneNumberKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .phoneNumber.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
